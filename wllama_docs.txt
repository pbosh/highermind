=====================================================
WLLAMA DOCS and READMES
=====================================================


Introducing Wllama V2.0
What's new
V2.0 introduces significant improvements in model management and caching. Key features include:

Completely rewritten model downloader with service worker
New ModelManager class providing comprehensive model handling and caching capabilities
Enhanced testing system built on the vitest framework
Added ModelManager
The new ModelManager class provides a robust interface for handling model files:

// Example usage
const modelManager = new ModelManager();

// List all models in cache
const cachedModels = await modelManager.getModels();

// Add a new model
const model = await modelManager.downloadModel('https://example.com/model.gguf');

// Check if model is valid (i.e. it is not corrupted)
// If status === ModelValidationStatus.VALID, you can use the model
// Otherwise, call model.refresh() to re-download it
const status = await model.validate();

// Re-download if needed (useful when remote model file has changed)
await model.refresh();

// Remove model from cache
await model.remove();

// Load the selected model into llama.cpp
const wllama = new Wllama(CONFIG_PATHS);
await wllama.loadModel(model);

// Alternatively, you can also pass directly model URL like in v1.x
// This will automatically download the model to cache
await wllama.loadModelFromUrl('https://example.com/model.gguf');
Key features of ModelManager:

Automatic handling of split GGUF models
Built-in model validation
Parallel downloads of model shards
Cache management with refresh and removal options
Added loadModelFromHF
A new helper function to load models directly from Hugging Face Hub. This is a convenient wrapper over loadModelFromUrl that handles HF repository URLs.

await wllama.loadModelFromHF(
  'ggml-org/models',
  'tinyllamas/stories260K.gguf'
);
Migration to v2.0
Simplified new Wllama() constructor
In v2.0, the configuration paths have been simplified. You now only need to specify the *.wasm files, as the *.js files are no longer required.

Previously in v1.x:

const CONFIG_PATHS = {
  'single-thread/wllama.js'       : '../../esm/single-thread/wllama.js',
  'single-thread/wllama.wasm'     : '../../esm/single-thread/wllama.wasm',
  'multi-thread/wllama.js'        : '../../esm/multi-thread/wllama.js',
  'multi-thread/wllama.wasm'      : '../../esm/multi-thread/wllama.wasm',
  'multi-thread/wllama.worker.mjs': '../../esm/multi-thread/wllama.worker.mjs',
};
const wllama = new Wllama(CONFIG_PATHS);
From v2.0:

// You only need to specify 2 files
const CONFIG_PATHS = {
  'single-thread/wllama.wasm': '../../esm/single-thread/wllama.wasm',
  'multi-thread/wllama.wasm' : '../../esm/multi-thread/wllama.wasm',
};
const wllama = new Wllama(CONFIG_PATHS);
Alternatively, you can use the *.wasm files from CDN:

import WasmFromCDN from '@wllama/wllama/esm/wasm-from-cdn.js';
const wllama = new Wllama(WasmFromCDN);
// NOTE: this is not recommended
// only use this when you can't embed wasm files in your project
The Wllama constructor now accepts an optional second parameter of type WllamaConfig for configuration options:

Important

Most configuration options previously available in DownloadModelConfig used with loadModelFromUrl() have been moved to this constructor config.

const wllama = new Wllama(CONFIG_PATHS, {
  parallelDownloads: 5, // maximum concurrent downloads
  allowOffline: false, // whether to allow offline model loading
});
Wllama.loadModelFromUrl
As mentioned earlier, some options are moved to Wllama constructor, including:

parallelDownloads
allowOffline
Other changes
Wllama.downloadModel is removed. Please use ModelManager.downloadModel instead
loadModelFromUrl won't check if cached model is up-to-date. You may need to manually call Model.refresh() to re-download the model.
Changes in CacheManager:
Added CacheManager.download function
CacheManager.open(nameOrURL) now accepts both file name and original URL. It now returns a Blob instead of a ReadableStream
Internal Changes
Notable internal improvements made to the codebase:

Comprehensive test coverage using vitest, with browser testing for Chrome and Firefox (Safari support planned for the future)
Enhanced CI pipeline including validation for example builds, ESM compilation and lint checks


=====================================================



@wllama/wllama
wllama - Wasm binding for llama.cpp


WebAssembly binding for llama.cpp

👉 Try the demo app

📄 Documentation

For changelog, please visit releases page

Important

Version 2.0 is released 👉 read more



Features
Typescript support
Can run inference directly on browser (using WebAssembly SIMD), no backend or GPU is needed!
No runtime dependency (see package.json)
High-level API: completions, embeddings
Low-level API: (de)tokenize, KV cache control, sampling control,...
Ability to split the model into smaller files and load them in parallel (same as split and cat)
Auto switch between single-thread and multi-thread build based on browser support
Inference is done inside a worker, does not block UI render
Pre-built npm package @wllama/wllama
Limitations:

To enable multi-thread, you must add Cross-Origin-Embedder-Policy and Cross-Origin-Opener-Policy headers. See this discussion for more details.
No WebGPU support, but maybe possible in the future
Max file size is 2GB, due to size restriction of ArrayBuffer. If your model is bigger than 2GB, please follow the Split model section below.
Code demo and documentation
📄 Documentation

Demo:

Basic usages with completions and embeddings: https://github.ngxson.com/wllama/examples/basic/
Embedding and cosine distance: https://github.ngxson.com/wllama/examples/embeddings/
For more advanced example using low-level API, have a look at test file: wllama.test.ts
How to use
Use Wllama inside React Typescript project
Install it:

npm i @wllama/wllama
Copy
Then, import the module:

import { Wllama } from '@wllama/wllama';
let wllamaInstance = new Wllama(WLLAMA_CONFIG_PATHS, ...);
// (the rest is the same with earlier example)
Copy
For complete code example, see examples/main/src/utils/wllama.context.tsx

NOTE: this example only covers completions usage. For embeddings, please see examples/embeddings/index.html

Prepare your model
It is recommended to split the model into chunks of maximum 512MB. This will result in slightly faster download speed (because multiple splits can be downloaded in parallel), and also prevent some out-of-memory issues.
See the "Split model" section below for more details.
It is recommended to use quantized Q4, Q5 or Q6 for balance among performance, file size and quality. Using IQ (with imatrix) is not recommended, may result in slow inference and low quality.
Simple usage with ES6 module
For complete code, see examples/basic/index.html

import { Wllama } from './esm/index.js';

(async () => {
  const CONFIG_PATHS = {
    'single-thread/wllama.wasm': './esm/single-thread/wllama.wasm',
    'multi-thread/wllama.wasm' : './esm/multi-thread/wllama.wasm',
  };
  // Automatically switch between single-thread and multi-thread version based on browser support
  // If you want to enforce single-thread, add { "n_threads": 1 } to LoadModelConfig
  const wllama = new Wllama(CONFIG_PATHS);
  // Define a function for tracking the model download progress
  const progressCallback =  ({ loaded, total }) => {
    // Calculate the progress as a percentage
    const progressPercentage = Math.round((loaded / total) * 100);
    // Log the progress in a user-friendly format
    console.log(`Downloading... ${progressPercentage}%`);
  };
  // Load GGUF from Hugging Face hub
  // (alternatively, you can use loadModelFromUrl if the model is not from HF hub)
  await wllama.loadModelFromHF(
    'ggml-org/models',
    'tinyllamas/stories260K.gguf',
    {
      progressCallback,
    }
  );
  const outputText = await wllama.createCompletion(elemInput.value, {
    nPredict: 50,
    sampling: {
      temp: 0.5,
      top_k: 40,
      top_p: 0.9,
    },
  });
  console.log(outputText);
})();
Copy
Alternatively, you can use the *.wasm files from CDN:

import WasmFromCDN from '@wllama/wllama/esm/wasm-from-cdn.js';
const wllama = new Wllama(WasmFromCDN);
// NOTE: this is not recommended, only use when you can't embed wasm files in your project
Copy
Split model
Cases where we want to split the model:

Due to size restriction of ArrayBuffer, the size limitation of a file is 2GB. If your model is bigger than 2GB, you can split the model into small files.
Even with a small model, splitting into chunks allows the browser to download multiple chunks in parallel, thus making the download process a bit faster.
We use llama-gguf-split to split a big gguf file into smaller files. You can download the pre-built binary via llama.cpp release page:

# Split the model into chunks of 512 Megabytes
./llama-gguf-split --split-max-size 512M ./my_model.gguf ./my_model
Copy
This will output files ending with -00001-of-00003.gguf, -00002-of-00003.gguf, and so on.

You can then pass to loadModelFromUrl or loadModelFromHF the URL of the first file and it will automatically load all the chunks:

const wllama = new Wllama(CONFIG_PATHS, {
  parallelDownloads: 5, // optional: maximum files to download in parallel (default: 3)
});
await wllama.loadModelFromHF(
  'ngxson/tinyllama_split_test',
  'stories15M-q8_0-00001-of-00003.gguf'
);
Copy
Custom logger (suppress debug messages)
When initializing Wllama, you can pass a custom logger to Wllama.

Example 1: Suppress debug message

import { Wllama, LoggerWithoutDebug } from '@wllama/wllama';

const wllama = new Wllama(pathConfig, {
  // LoggerWithoutDebug is predefined inside wllama
  logger: LoggerWithoutDebug,
});
Copy
Example 2: Add emoji prefix to log messages

const wllama = new Wllama(pathConfig, {
  logger: {
    debug: (...args) => console.debug('🔧', ...args),
    log: (...args) => console.log('ℹ️', ...args),
    warn: (...args) => console.warn('⚠️', ...args),
    error: (...args) => console.error('☠️', ...args),
  },
});
Copy
How to compile the binary yourself
This repository already come with pre-built binary from llama.cpp source code. However, in some cases you may want to compile it yourself:

You don't trust the pre-built one.
You want to try out latest - bleeding-edge changes from upstream llama.cpp source code.
You can use the commands below to compile it yourself:

# /!\ IMPORTANT: Require having docker compose installed

# Clone the repository with submodule
git clone --recurse-submodules https://github.com/ngxson/wllama.git
cd wllama

# Optionally, you can run this command to update llama.cpp to latest upstream version (bleeding-edge, use with your own risk!)
# git submodule update --remote --merge

# Install the required modules
npm i

# Firstly, build llama.cpp into wasm
npm run build:wasm
# Then, build ES module
npm run build
Copy
TODO
Add support for LoRA adapter
Support GPU inference via WebGL
Support multi-sequences: knowing the resource limitation when using WASM, I don't think having multi-sequences is a good idea
Multi-modal: Waiting for refactoring LLaVA implementation from llama.cpp


=====================================================


@wllama/wllama
@wllama/wllama

E= Enumerations
C= Classes
I= Interfaces
T= Type Aliases
V= Variables


E
ModelValidationStatus
C
Model
C
ModelManager
C
Wllama
C
WllamaError
I
AssetsPathConfig
I
CacheEntry
I
CacheEntryMetadata
I
ChatCompletionOptions
I
ContextOptions
I
LoadedContextInfo
I
LoadModelConfig
I
ModelManagerParams
I
ModelMetadata
I
SamplingConfig
I
WllamaChatMessage
I
WllamaConfig
I
WllamaLogger
T
DownloadOptions
T
DownloadProgressCallback
T
WllamaErrorType
V
LoggerWithoutDebug
V
POLYFILL_ETAG
E
ModelValidationStatus
C
Model
C
ModelManager
C
Wllama
C
WllamaError
I
AssetsPathConfig
I
CacheEntry
I
CacheEntryMetadata
I
ChatCompletionOptions
I
ContextOptions
I
LoadedContextInfo
I
LoadModelConfig
I
ModelManagerParams
I
ModelMetadata
I
SamplingConfig
I
WllamaChatMessage
I
WllamaConfig
I
WllamaLogger
T
DownloadOptions
T
DownloadProgressCallback
T
WllamaErrorType
V
LoggerWithoutDebug
V
POLYFILL_ETAG
@wllama/wllama
ModelValidationStatus
Model
ModelManager
Wllama
WllamaError
AssetsPathConfig
CacheEntry
CacheEntryMetadata
ChatCompletionOptions
ContextOptions
LoadedContextInfo
LoadModelConfig
ModelManagerParams
ModelMetadata
SamplingConfig
WllamaChatMessage
WllamaConfig
WllamaLogger
DownloadOptions
DownloadProgressCallback
WllamaErrorType
LoggerWithoutDebug
POLYFILL_ETAG
Generated using TypeDoc


=====================================================


@wllama/wllamaModelValidationStatus
Enumeration ModelValidationStatus
Status of the model validation

Defined in src/model-manager.ts:24
Enumeration Members
P
DELETED
P
INVALID
P
VALID
DELETED
DELETED: "deleted"
Defined in src/model-manager.ts:27
INVALID
INVALID: "invalid"
Defined in src/model-manager.ts:26
VALID
VALID: "valid"
Defined in src/model-manager.ts:25


=====================================================


@wllama/wllamaModel
Class Model
Model class

One model can have multiple shards, each shard is a GGUF file.

Defined in src/model-manager.ts:55
Constructors
C
constructor
Properties
P
files
P
size
P
url
Methods
M
open
M
refresh
M
remove
M
validate
constructor
new Model(
    modelManager: ModelManager,
    url: string,
    savedFiles?: CacheEntry[],
): Model
Parameters
modelManager: ModelManager
url: string
OptionalsavedFiles: CacheEntry[]
Returns Model
Defined in src/model-manager.ts:57
files
files: CacheEntry[]
List of all shards in the cache, sorted by original URL (ascending order)

Defined in src/model-manager.ts:89
size
size: number
Size in bytes (total size of all shards).

A value of -1 means the model is deleted from the cache. You must call ModelManager.downloadModel to re-download the model.

Defined in src/model-manager.ts:85
url
url: string
URL to the GGUF file (in case it contains multiple shards, the URL should point to the first shard)

This URL will be used to identify the model in the cache. There can't be 2 models with the same URL.

Defined in src/model-manager.ts:79
open
open(): Promise<Blob[]>
Open and get a list of all shards as Blobs

Returns Promise<Blob[]>
Defined in src/model-manager.ts:93
refresh
refresh(options?: DownloadOptions): Promise<void>
In case the model is invalid, call this function to re-download the model

Parameters
options: DownloadOptions = {}
Returns Promise<void>
Defined in src/model-manager.ts:139
remove
remove(): Promise<void>
Remove the model from the cache

Returns Promise<void>
Defined in src/model-manager.ts:178
validate
validate(): ModelValidationStatus
Validate the model files.

If the model is invalid, the model manager will not be able to use it. You must call refresh to re-download the model.

Cases that model is invalid:

The model is deleted from the cache
The model files are missing (or the download is interrupted)
Returns ModelValidationStatus
Defined in src/model-manager.ts:121


=====================================================


@wllama/wllamaModelManager
Class ModelManager
Defined in src/model-manager.ts:213
Constructors
C
constructor
Properties
P
cacheManager
P
logger
P
params
Methods
M
clear
M
downloadModel
M
getModelOrDownload
M
getModels
M
parseModelUrl
constructor
new ModelManager(params?: ModelManagerParams): ModelManager
Parameters
params: ModelManagerParams = {}
Returns ModelManager
Defined in src/model-manager.ts:220
cacheManager
cacheManager: CacheManager
Defined in src/model-manager.ts:215
logger
logger: WllamaLogger
Defined in src/model-manager.ts:218
params
params: ModelManagerParams
Defined in src/model-manager.ts:217
clear
clear(): Promise<void>
Remove all models from the cache

Returns Promise<void>
Defined in src/model-manager.ts:316
downloadModel
downloadModel(url: string, options?: DownloadOptions): Promise<Model>
Download a model from the given URL.

The URL must end with .gguf

Parameters
url: string
options: DownloadOptions = {}
Returns Promise<Model>
Defined in src/model-manager.ts:279
getModelOrDownload
getModelOrDownload(url: string, options?: DownloadOptions): Promise<Model>
Get a model from the cache or download it if it's not available.

Parameters
url: string
options: DownloadOptions = {}
Returns Promise<Model>
Defined in src/model-manager.ts:300
getModels
getModels(opts?: { includeInvalid?: boolean }): Promise<Model[]>
Get all models in the cache

Parameters
opts: { includeInvalid?: boolean } = {}
Returns Promise<Model[]>
Defined in src/model-manager.ts:255
Static
parseModelUrl
parseModelUrl(modelUrl: string | string[]): string[]
Parses a model URL and returns an array of URLs based on the following patterns:

If the input URL is an array, it returns the array itself.
If the input URL is a string in the gguf-split format, it returns an array containing the URL of each shard in ascending order.
Otherwise, it returns an array containing the input URL as a single element array.
Parameters
modelUrl: string | string[]
URL or list of URLs

Returns string[]
Defined in src/model-manager.ts:233


=====================================================


@wllama/wllamaWllama
Class Wllama
Defined in src/wllama.ts:204
Constructors
C
constructor
Properties
P
cacheManager
P
modelManager
Methods
M
_getDebugInfo
M
createChatCompletion
M
createCompletion
M
createEmbedding
M
decode
M
detokenize
M
embeddings
M
encode
M
exit
M
formatChat
M
getBOS
M
getChatTemplate
M
getDecoderStartToken
M
getEOS
M
getEOT
M
getLoadedContextInfo
M
getLogits
M
getModelMetadata
M
getVocab
M
isEncoderDecoderArchitecture
M
isModelLoaded
M
isMultithread
M
isTokenEOG
M
kvClear
M
kvRemove
M
loadModel
M
loadModelFromHF
M
loadModelFromUrl
M
lookupToken
M
mustAddBosToken
M
mustAddEosToken
M
samplingAccept
M
samplingInit
M
samplingSample
M
sessionLoad
M
sessionSave
M
setOptions
M
tokenize
constructor
new Wllama(pathConfig: AssetsPathConfig, wllamaConfig?: WllamaConfig): Wllama
Parameters
pathConfig: AssetsPathConfig
wllamaConfig: WllamaConfig = {}
Returns Wllama
Defined in src/wllama.ts:229
cacheManager
cacheManager: CacheManager
Defined in src/wllama.ts:206
modelManager
modelManager: ModelManager
Defined in src/wllama.ts:207
_getDebugInfo
_getDebugInfo(): Promise<any>
get debug info

Returns Promise<any>
Defined in src/wllama.ts:1063
createChatCompletion
createChatCompletion(
    messages: WllamaChatMessage[],
    options: ChatCompletionOptions,
): Promise<string>
Make completion for a given chat messages.

NOTE: this function uses the chat template (if available) to format the chat messages. If the template is not available, it will use the default format (chatml). It can throw an error if the chat template is not compatible.

Parameters
messages: WllamaChatMessage[]
Chat messages

options: ChatCompletionOptions
Returns Promise<string>
Output completion text (only the completion part)

Defined in src/wllama.ts:607
createCompletion
createCompletion(
    prompt: string,
    options: ChatCompletionOptions,
): Promise<string>
Make completion for a given text.

Parameters
prompt: string
Input text

options: ChatCompletionOptions
Returns Promise<string>
Output completion text (only the completion part)

Defined in src/wllama.ts:621
createEmbedding
createEmbedding(
    text: string,
    options?: { skipBOS?: boolean; skipEOS?: boolean },
): Promise<number[]>
Calculate embedding vector for a given text. By default, BOS and EOS tokens will be added automatically. You can use the "skipBOS" and "skipEOS" option to disable it.

Parameters
text: string
Input text

options: { skipBOS?: boolean; skipEOS?: boolean } = {}
Returns Promise<number[]>
An embedding vector

Defined in src/wllama.ts:572
decode
decode(
    tokens: number[],
    options: { skipLogits?: boolean },
): Promise<{ nPast: number }>
Run llama_decode()

Parameters
tokens: number[]
A list of tokens to be decoded

options: { skipLogits?: boolean }
Returns Promise<{ nPast: number }>
n_past (number of tokens so far in the sequence)

Defined in src/wllama.ts:759
detokenize
detokenize(tokens: number[]): Promise<Uint8Array>
Convert a list of tokens to text

Parameters
tokens: number[]
Returns Promise<Uint8Array>
Uint8Array, which maybe an unfinished unicode

Defined in src/wllama.ts:747
embeddings
embeddings(tokens: number[]): Promise<number[]>
Calculate embeddings for a given list of tokens. Output vector is always normalized

Parameters
tokens: number[]
Returns Promise<number[]>
A list of number represents an embedding vector of N dimensions

Defined in src/wllama.ts:910
encode
encode(
    tokens: number[],
    options?: Record<never, never>,
): Promise<{ nPast: number }>
Run llama_encode()

Parameters
tokens: number[]
A list of tokens to be encoded

Optionaloptions: Record<never, never>
Unused for now

Returns Promise<{ nPast: number }>
n_past (number of tokens so far in the sequence)

Defined in src/wllama.ts:811
exit
exit(): Promise<void>
Unload the model and free all memory.

Note: This function will NOT crash if model is not yet loaded

Returns Promise<void>
Defined in src/wllama.ts:1055
formatChat
formatChat(
    messages: WllamaChatMessage[],
    addAssistant: boolean,
    template?: string,
): Promise<string>
Apply chat template to a list of messages

Parameters
messages: WllamaChatMessage[]
list of messages

addAssistant: boolean
whether to add assistant prompt at the end

Optionaltemplate: string
(optional) custom template, see llama-server --chat-template argument for more details

Returns Promise<string>
formatted chat

Defined in src/wllama.ts:1022
getBOS
getBOS(): number
Get token ID associated to BOS (begin of sentence) token.

NOTE: This can only being used after loadModel is called.

Returns number
-1 if the model is not loaded.

Defined in src/wllama.ts:272
getChatTemplate
getChatTemplate(): null | string
Get the jinja chat template comes with the model. It only available if the original model (before converting to gguf) has the template in tokenizer_config.json

NOTE: This can only being used after loadModel is called.

Returns null | string
the jinja template. null if there is no template in gguf

Defined in src/wllama.ts:390
getDecoderStartToken
getDecoderStartToken(): number
Get token ID associated to token used by decoder, to start generating output sequence(only usable for encoder-decoder architecture). In other words, encoder uses normal BOS and decoder uses this token.

NOTE: This can only being used after loadModel is called.

Returns number
-1 if the model is not loaded.

Defined in src/wllama.ts:319
getEOS
getEOS(): number
Get token ID associated to EOS (end of sentence) token.

NOTE: This can only being used after loadModel is called.

Returns number
-1 if the model is not loaded.

Defined in src/wllama.ts:283
getEOT
getEOT(): number
Get token ID associated to EOT (end of turn) token.

NOTE: This can only being used after loadModel is called.

Returns number
-1 if the model is not loaded.

Defined in src/wllama.ts:294
getLoadedContextInfo
getLoadedContextInfo(): LoadedContextInfo
Returns LoadedContextInfo
Defined in src/wllama.ts:554
getLogits
getLogits(topK?: number): Promise<{ p: number; token: number }[]>
Get softmax-ed probability of logits, can be used for custom sampling

Parameters
topK: number = 40
Get top K tokens having highest logits value. If topK == -1, we return all n_vocab logits, but this is not recommended because it's slow.

Returns Promise<{ p: number; token: number }[]>
Defined in src/wllama.ts:898
getModelMetadata
getModelMetadata(): ModelMetadata
Get model hyper-parameters and metadata

NOTE: This can only being used after loadModel is called.

Returns ModelMetadata
ModelMetadata

Defined in src/wllama.ts:330
getVocab
getVocab(): Promise<Uint8Array[]>
Get a list of pieces in vocab. NOTE: This function is slow, should only be used once.

Returns Promise<Uint8Array[]>
A list of Uint8Array. The nth element in the list associated to nth token in vocab

Defined in src/wllama.ts:705
isEncoderDecoderArchitecture
isEncoderDecoderArchitecture(): boolean
Check if the current model uses encoder-decoder architecture

NOTE: This can only being used after loadModel is called.

Returns boolean
true if multi-thread is used.

Defined in src/wllama.ts:354
isModelLoaded
isModelLoaded(): boolean
Check if the model is loaded via loadModel()

Returns boolean
Defined in src/wllama.ts:261
isMultithread
isMultithread(): boolean
Check if we're currently using multi-thread build.

NOTE: This can only being used after loadModel is called.

Returns boolean
true if multi-thread is used.

Defined in src/wllama.ts:342
isTokenEOG
isTokenEOG(token: number): boolean
Check if a given token is end-of-generation token (e.g. EOS, EOT, etc.)

Parameters
token: number
the token ID to be checked

Returns boolean
true if the token is EOS, EOT, or any other end-of-generation tokens

Defined in src/wllama.ts:304
kvClear
kvClear(): Promise<void>
Clear all tokens in KV cache

Returns Promise<void>
Defined in src/wllama.ts:972
kvRemove
kvRemove(nKeep: number, nDiscard: number): Promise<void>
Remove and shift some tokens from KV cache. Keep n_keep, remove n_discard then shift the rest

Parameters
nKeep: number
nDiscard: number
Returns Promise<void>
Defined in src/wllama.ts:957
loadModel
loadModel(
    ggufBlobsOrModel: Blob[] | Model,
    config?: LoadModelConfig,
): Promise<void>
Load model from a given list of Blob.

You can pass multiple buffers into the function (in case the model contains multiple shards).

Parameters
ggufBlobsOrModel: Blob[] | Model
Can be either list of Blobs (in case you use local file), or a Model object (in case you use ModelManager)

config: LoadModelConfig = {}
LoadModelConfig

Returns Promise<void>
Defined in src/wllama.ts:447
loadModelFromHF
loadModelFromHF(
    modelId: string,
    filePath: string,
    config?: LoadModelConfig & DownloadOptions & { useCache?: boolean },
): Promise<void>
Load model from a given Hugging Face model ID and file path.

Parameters
modelId: string
The HF model ID, for example: 'ggml-org/models'

filePath: string
The GGUF file path, for example: 'tinyllamas/stories15M-q4_0.gguf'

config: LoadModelConfig & DownloadOptions & { useCache?: boolean } = {}
Returns Promise<void>
Defined in src/wllama.ts:422
loadModelFromUrl
loadModelFromUrl(
    modelUrl: string | string[],
    config?: LoadModelConfig & DownloadOptions & { useCache?: boolean },
): Promise<void>
Load model from a given URL (or a list of URLs, in case the model is splitted into smaller files)

If the model already been downloaded (via downloadModel()), then we will use the cached model
Else, we download the model from internet
Parameters
modelUrl: string | string[]
URL to the GGUF file. If the model is splitted, pass the URL to the first shard.

config: LoadModelConfig & DownloadOptions & { useCache?: boolean } = {}
Returns Promise<void>
Defined in src/wllama.ts:402
lookupToken
lookupToken(piece: string): Promise<number>
Lookup to see if a token exist in vocab or not. Useful for searching special tokens like "<|im_start|>" NOTE: It will match the whole token, so do not use it as a replacement for tokenize()

Parameters
piece: string
Returns Promise<number>
Token ID associated to the given piece. Returns -1 if cannot find the token.

Defined in src/wllama.ts:717
mustAddBosToken
mustAddBosToken(): boolean
Must we add BOS token to the tokenized sequence?

NOTE: This can only being used after loadModel is called.

Returns boolean
true if BOS token must be added to the sequence

Defined in src/wllama.ts:366
mustAddEosToken
mustAddEosToken(): boolean
Must we add EOS token to the tokenized sequence?

NOTE: This can only being used after loadModel is called.

Returns boolean
true if EOS token must be added to the sequence

Defined in src/wllama.ts:378
samplingAccept
samplingAccept(tokens: number[]): Promise<void>
Accept and save a new token to ctx_sampling

Parameters
tokens: number[]
Returns Promise<void>
Defined in src/wllama.ts:886
samplingInit
samplingInit(config: SamplingConfig, pastTokens?: number[]): Promise<void>
Create or reset the ctx_sampling

Parameters
config: SamplingConfig
pastTokens: number[] = []
In case re-initializing the ctx_sampling, you can re-import past tokens into the new context

Returns Promise<void>
Defined in src/wllama.ts:685
samplingSample
samplingSample(): Promise<{ piece: Uint8Array; token: number }>
Sample a new token (remember to samplingInit() at least once before calling this function)

Returns Promise<{ piece: Uint8Array; token: number }>
the token ID and its detokenized value (which maybe an unfinished unicode)

Defined in src/wllama.ts:873
sessionLoad
sessionLoad(filePath: string): Promise<void>
Load session from file (virtual file system) TODO: add ability to download the file

Parameters
filePath: string
Returns Promise<void>
Defined in src/wllama.ts:1000
sessionSave
sessionSave(filePath: string): Promise<{ tokens: number[] }>
Save session to file (virtual file system) TODO: add ability to download the file

Parameters
filePath: string
Returns Promise<{ tokens: number[] }>
List of tokens saved to the file

Defined in src/wllama.ts:987
setOptions
setOptions(opt: ContextOptions): Promise<void>
Set options for underlaying llama_context

Parameters
opt: ContextOptions
Returns Promise<void>
Defined in src/wllama.ts:1044
tokenize
tokenize(text: string, special?: boolean): Promise<number[]>
Convert a given text to list of tokens

Parameters
text: string
special: boolean = true
Should split special tokens?

Returns Promise<number[]>
List of token ID

Defined in src/wllama.ts:733


=====================================================


@wllama/wllamaWllamaError
Class WllamaError
Hierarchy
Error
WllamaError
Defined in src/wllama.ts:196
Constructors
C
constructor
Properties
P
type
constructor
new WllamaError(message: string, type?: WllamaErrorType): WllamaError
Parameters
message: string
type: WllamaErrorType = 'unknown_error'
Returns WllamaError
Overrides Error.constructor

Defined in src/wllama.ts:198
type
type: WllamaErrorType
Defined in src/wllama.ts:197


=====================================================


@wllama/wllamaAssetsPathConfig
Interface AssetsPathConfig
interface AssetsPathConfig {
    "multi-thread/wllama.wasm"?: string;
    "single-thread/wllama.wasm": string;
}
Defined in src/wllama.ts:63
Properties
P
multi-thread/wllama.wasm?
P
single-thread/wllama.wasm
Optional
multi-thread/wllama.wasm
"multi-thread/wllama.wasm"?: string
Defined in src/wllama.ts:65
single-thread/wllama.wasm
"single-thread/wllama.wasm": string
Defined in src/wllama.ts:64


=====================================================


@wllama/wllamaCacheEntry
Interface CacheEntry
interface CacheEntry {
    metadata: CacheEntryMetadata;
    name: string;
    size: number;
}
Defined in src/cache-manager.ts:25
Properties
P
metadata
P
name
P
size
metadata
metadata: CacheEntryMetadata
Other metadata

Defined in src/cache-manager.ts:37
name
name: string
File name in OPFS, in the format: ${hashSHA1(fullURL)}_${fileName}

Defined in src/cache-manager.ts:29
size
size: number
Size of file (in bytes)

Defined in src/cache-manager.ts:33
P
metadata
P
name
P
size
@wllama/wllama
ModelValidationStatus
Model
ModelManager
Wllama
WllamaError
AssetsPathConfig
CacheEntry
CacheEntryMetadata
ChatCompletionOptions
ContextOptions
LoadedContextInfo
LoadModelConfig
ModelManagerParams
ModelMetadata
SamplingConfig
WllamaChatMessage
WllamaConfig
WllamaLogger
DownloadOptions
DownloadProgressCallback
WllamaErrorType
LoggerWithoutDebug
POLYFILL_ETAG



=====================================================


@wllama/wllamaCacheEntryMetadata
Interface CacheEntryMetadata
interface CacheEntryMetadata {
    etag: string;
    originalSize: number;
    originalURL: string;
}
Defined in src/cache-manager.ts:40
Properties
P
etag
P
originalSize
P
originalURL
etag
etag: string
ETag header from remote request https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/ETag

Defined in src/cache-manager.ts:45
originalSize
originalSize: number
Remote file size (in bytes), used for integrity check

Defined in src/cache-manager.ts:49
originalURL
originalURL: string
Original URL of the remote model. Unused for now

Defined in src/cache-manager.ts:53


=====================================================


@wllama/wllamaChatCompletionOptions
Interface ChatCompletionOptions
interface ChatCompletionOptions {
    nPredict?: number;
    sampling?: SamplingConfig;
    stopTokens?: number[];
    useCache?: boolean;
    onNewToken(
        token: number,
        piece: Uint8Array,
        currentText: string,
        optionals: { abortSignal: () => any },
    ): any;
}
Defined in src/wllama.ts:121
Properties
P
nPredict?
P
sampling?
P
stopTokens?
P
useCache?
Methods
M
onNewToken?
Optional
nPredict
nPredict?: number
Defined in src/wllama.ts:122
Optional
sampling
sampling?: SamplingConfig
Defined in src/wllama.ts:131
Optional
stopTokens
stopTokens?: number[]
List of custom token IDs for stopping the generation. Note: To convert from text to token ID, use lookupToken()

Defined in src/wllama.ts:136
Optional
useCache
useCache?: boolean
Equivalent to cache_prompt option in llama.cpp server. Useful for chat, because it skip evaluating the history part of the conversation.

Defined in src/wllama.ts:141
Optional
onNewToken
onNewToken(
    token: number,
    piece: Uint8Array,
    currentText: string,
    optionals: { abortSignal: () => any },
): any
Parameters
token: number
piece: Uint8Array
currentText: string
optionals: { abortSignal: () => any }
Returns any
Defined in src/wllama.ts:123


=====================================================


@wllama/wllamaContextOptions
Interface ContextOptions
interface ContextOptions {
    embeddings: boolean;
}
Defined in src/wllama.ts:154
Properties
P
embeddings
embeddings
embeddings: boolean
Allow switching between embeddings / generation mode. Useful for models like GritLM.

Defined in src/wllama.ts:158


=====================================================


@wllama/wllamaLoadedContextInfo
Interface LoadedContextInfo
interface LoadedContextInfo {
    add_bos_token: boolean;
    add_eos_token: boolean;
    has_encoder: boolean;
    list_tokens_eog: number[];
    metadata: Record<string, string>;
    n_batch: number;
    n_ctx: number;
    n_ctx_train: number;
    n_embd: number;
    n_layer: number;
    n_ubatch: number;
    n_vocab: number;
    token_bos: number;
    token_decoder_start: number;
    token_eos: number;
    token_eot: number;
}
Defined in src/wllama.ts:161
Properties
P
add_bos_token
P
add_eos_token
P
has_encoder
P
list_tokens_eog
P
metadata
P
n_batch
P
n_ctx
P
n_ctx_train
P
n_embd
P
n_layer
P
n_ubatch
P
n_vocab
P
token_bos
P
token_decoder_start
P
token_eos
P
token_eot
add_bos_token
add_bos_token: boolean
Defined in src/wllama.ts:176
add_eos_token
add_eos_token: boolean
Defined in src/wllama.ts:177
has_encoder
has_encoder: boolean
Defined in src/wllama.ts:174
list_tokens_eog
list_tokens_eog: number[]
Defined in src/wllama.ts:173
metadata
metadata: Record<string, string>
Defined in src/wllama.ts:169
n_batch
n_batch: number
Defined in src/wllama.ts:164
n_ctx
n_ctx: number
Defined in src/wllama.ts:163
n_ctx_train
n_ctx_train: number
Defined in src/wllama.ts:166
n_embd
n_embd: number
Defined in src/wllama.ts:167
n_layer
n_layer: number
Defined in src/wllama.ts:168
n_ubatch
n_ubatch: number
Defined in src/wllama.ts:165
n_vocab
n_vocab: number
Defined in src/wllama.ts:162
token_bos
token_bos: number
Defined in src/wllama.ts:170
token_decoder_start
token_decoder_start: number
Defined in src/wllama.ts:175
token_eos
token_eos: number
Defined in src/wllama.ts:171
token_eot
token_eot: number
Defined in src/wllama.ts:172


=====================================================


@wllama/wllamaLoadModelConfig
Interface LoadModelConfig
interface LoadModelConfig {
    cache_type_k?:
        | "f32"
        | "f16"
        | "q8_0"
        | "q5_1"
        | "q5_0"
        | "q4_1"
        | "q4_0";
    cache_type_v?: "f32"
    | "f16"
    | "q8_0"
    | "q5_1"
    | "q5_0"
    | "q4_1"
    | "q4_0";
    embeddings?: boolean;
    n_batch?: number;
    n_ctx?: number;
    n_threads?: number;
    offload_kqv?: boolean;
    pooling_type?:
        | "LLAMA_POOLING_TYPE_UNSPECIFIED"
        | "LLAMA_POOLING_TYPE_NONE"
        | "LLAMA_POOLING_TYPE_MEAN"
        | "LLAMA_POOLING_TYPE_CLS";
    rope_freq_base?: number;
    rope_freq_scale?: number;
    rope_scaling_type?: | "LLAMA_ROPE_SCALING_TYPE_UNSPECIFIED"
    | "LLAMA_ROPE_SCALING_TYPE_NONE"
    | "LLAMA_ROPE_SCALING_TYPE_LINEAR"
    | "LLAMA_ROPE_SCALING_TYPE_YARN";
    seed?: number;
    yarn_attn_factor?: number;
    yarn_beta_fast?: number;
    yarn_beta_slow?: number;
    yarn_ext_factor?: number;
    yarn_orig_ctx?: number;
}
Defined in src/wllama.ts:68
Properties
P
cache_type_k?
P
cache_type_v?
P
embeddings?
P
n_batch?
P
n_ctx?
P
n_threads?
P
offload_kqv?
P
pooling_type?
P
rope_freq_base?
P
rope_freq_scale?
P
rope_scaling_type?
P
seed?
P
yarn_attn_factor?
P
yarn_beta_fast?
P
yarn_beta_slow?
P
yarn_ext_factor?
P
yarn_orig_ctx?
Optional
cache_type_k
cache_type_k?: "f32" | "f16" | "q8_0" | "q5_1" | "q5_0" | "q4_1" | "q4_0"
Defined in src/wllama.ts:96
Optional
cache_type_v
cache_type_v?: "f32" | "f16" | "q8_0" | "q5_1" | "q5_0" | "q4_1" | "q4_0"
Defined in src/wllama.ts:97
Optional
embeddings
embeddings?: boolean
Defined in src/wllama.ts:74
Optional
n_batch
n_batch?: number
Defined in src/wllama.ts:71
Optional
n_ctx
n_ctx?: number
Defined in src/wllama.ts:70
Optional
n_threads
n_threads?: number
Defined in src/wllama.ts:73
Optional
offload_kqv
offload_kqv?: boolean
Defined in src/wllama.ts:75
Optional
pooling_type
pooling_type?:
    | "LLAMA_POOLING_TYPE_UNSPECIFIED"
    | "LLAMA_POOLING_TYPE_NONE"
    | "LLAMA_POOLING_TYPE_MEAN"
    | "LLAMA_POOLING_TYPE_CLS"
Defined in src/wllama.ts:76
Optional
rope_freq_base
rope_freq_base?: number
Defined in src/wllama.ts:87
Optional
rope_freq_scale
rope_freq_scale?: number
Defined in src/wllama.ts:88
Optional
rope_scaling_type
rope_scaling_type?:
    | "LLAMA_ROPE_SCALING_TYPE_UNSPECIFIED"
    | "LLAMA_ROPE_SCALING_TYPE_NONE"
    | "LLAMA_ROPE_SCALING_TYPE_LINEAR"
    | "LLAMA_ROPE_SCALING_TYPE_YARN"
Defined in src/wllama.ts:82
Optional
seed
seed?: number
Defined in src/wllama.ts:69
Optional
yarn_attn_factor
yarn_attn_factor?: number
Defined in src/wllama.ts:90
Optional
yarn_beta_fast
yarn_beta_fast?: number
Defined in src/wllama.ts:91
Optional
yarn_beta_slow
yarn_beta_slow?: number
Defined in src/wllama.ts:92
Optional
yarn_ext_factor
yarn_ext_factor?: number
Defined in src/wllama.ts:89
Optional
yarn_orig_ctx
yarn_orig_ctx?: number
Defined in src/wllama.ts:93


=====================================================


@wllama/wllamaModelManagerParams
Interface ModelManagerParams
Parameters for ModelManager constructor

interface ModelManagerParams {
    allowOffline?: boolean;
    cacheManager?: CacheManager;
    logger?: WllamaLogger;
    parallelDownloads?: number;
}
Defined in src/model-manager.ts:33
Properties
P
allowOffline?
P
cacheManager?
P
logger?
P
parallelDownloads?
Optional
allowOffline
allowOffline?: boolean
Allow offline mode

Default: false

Defined in src/model-manager.ts:47
Optional
cacheManager
cacheManager?: CacheManager
Defined in src/model-manager.ts:34
Optional
logger
logger?: WllamaLogger
Defined in src/model-manager.ts:35
Optional
parallelDownloads
parallelDownloads?: number
Number of parallel downloads

Default: 3

Defined in src/model-manager.ts:41


=====================================================


@wllama/wllamaModelMetadata
Interface ModelMetadata
interface ModelMetadata {
    hparams: {
        nCtxTrain: number;
        nEmbd: number;
        nLayer: number;
        nVocab: number;
    };
    meta: Record<string, string>;
}
Defined in src/wllama.ts:144
Properties
P
hparams
P
meta
hparams
hparams: { nCtxTrain: number; nEmbd: number; nLayer: number; nVocab: number }
Defined in src/wllama.ts:145
meta
meta: Record<string, string>
Defined in src/wllama.ts:151


=====================================================


@wllama/wllamaSamplingConfig
Interface SamplingConfig
interface SamplingConfig {
    dynatemp_exponent?: number;
    dynatemp_range?: number;
    grammar?: string;
    logit_bias?: { bias: number; token: number }[];
    min_p?: number;
    mirostat?: number;
    mirostat_tau?: number;
    n_prev?: number;
    n_probs?: number;
    penalty_freq?: number;
    penalty_last_n?: number;
    penalty_present?: number;
    penalty_repeat?: number;
    temp?: number;
    top_k?: number;
    top_p?: number;
    typical_p?: number;
}
Defined in src/wllama.ts:100
Properties
P
dynatemp_exponent?
P
dynatemp_range?
P
grammar?
P
logit_bias?
P
min_p?
P
mirostat?
P
mirostat_tau?
P
n_prev?
P
n_probs?
P
penalty_freq?
P
penalty_last_n?
P
penalty_present?
P
penalty_repeat?
P
temp?
P
top_k?
P
top_p?
P
typical_p?
Optional
dynatemp_exponent
dynatemp_exponent?: number
Defined in src/wllama.ts:112
Optional
dynatemp_range
dynatemp_range?: number
Defined in src/wllama.ts:111
Optional
grammar
grammar?: string
Defined in src/wllama.ts:113
Optional
logit_bias
logit_bias?: { bias: number; token: number }[]
Defined in src/wllama.ts:118
Optional
min_p
min_p?: number
Defined in src/wllama.ts:116
Optional
mirostat
mirostat?: number
Defined in src/wllama.ts:102
Optional
mirostat_tau
mirostat_tau?: number
Defined in src/wllama.ts:103
Optional
n_prev
n_prev?: number
Defined in src/wllama.ts:114
Optional
n_probs
n_probs?: number
Defined in src/wllama.ts:115
Optional
penalty_freq
penalty_freq?: number
Defined in src/wllama.ts:109
Optional
penalty_last_n
penalty_last_n?: number
Defined in src/wllama.ts:107
Optional
penalty_present
penalty_present?: number
Defined in src/wllama.ts:110
Optional
penalty_repeat
penalty_repeat?: number
Defined in src/wllama.ts:108
Optional
temp
temp?: number
Defined in src/wllama.ts:104
Optional
top_k
top_k?: number
Defined in src/wllama.ts:106
Optional
top_p
top_p?: number
Defined in src/wllama.ts:105
Optional
typical_p
typical_p?: number
Defined in src/wllama.ts:117


=====================================================


@wllama/wllamaWllamaChatMessage
Interface WllamaChatMessage
interface WllamaChatMessage {
    content: string;
    role: "system" | "user" | "assistant";
}
Defined in src/wllama.ts:58
Properties
P
content
P
role
content
content: string
Defined in src/wllama.ts:60
role
role: "system" | "user" | "assistant"
Defined in src/wllama.ts:59


=====================================================


@wllama/wllamaWllamaConfig
Interface WllamaConfig
interface WllamaConfig {
    allowOffline?: boolean;
    cacheManager?: CacheManager;
    logger?: WllamaLogger;
    modelManager?: ModelManager;
    parallelDownloads?: number;
    suppressNativeLog?: boolean;
}
Defined in src/wllama.ts:27
Properties
P
allowOffline?
P
cacheManager?
P
logger?
P
modelManager?
P
parallelDownloads?
P
suppressNativeLog?
Optional
allowOffline
allowOffline?: boolean
Allow offline mode. If true, the model will be loaded from cache if it's available.

Default: allowOffline = false

Defined in src/wllama.ts:47
Optional
cacheManager
cacheManager?: CacheManager
Custom cache manager (only for advanced usage)

Defined in src/wllama.ts:51
Optional
logger
logger?: WllamaLogger
Custom logger functions

Defined in src/wllama.ts:35
Optional
modelManager
modelManager?: ModelManager
Custom model manager (only for advanced usage)

Defined in src/wllama.ts:55
Optional
parallelDownloads
parallelDownloads?: number
Maximum number of parallel files to be downloaded

Default: parallelDownloads = 3

Defined in src/wllama.ts:41
Optional
suppressNativeLog
suppressNativeLog?: boolean
If true, suppress all log messages from native CPP code

Defined in src/wllama.ts:31


=====================================================


@wllama/wllamaWllamaLogger
Interface WllamaLogger
interface WllamaLogger {
    debug: {
        (...data: any[]): void;
        (message?: any, ...optionalParams: any[]): void;
    };
    error: {
        (...data: any[]): void;
        (message?: any, ...optionalParams: any[]): void;
    };
    log: {
        (...data: any[]): void;
        (message?: any, ...optionalParams: any[]): void;
    };
    warn: {
        (...data: any[]): void;
        (message?: any, ...optionalParams: any[]): void;
    };
}
Defined in src/wllama.ts:19
Properties
P
debug
P
error
P
log
P
warn
debug
debug: {
    (...data: any[]): void;
    (message?: any, ...optionalParams: any[]): void;
}
Type declaration
(...data: any[]): void
MDN Reference

Parameters
...data: any[]
Returns void
(message?: any, ...optionalParams: any[]): void
The console.debug() function is an alias for log.

Parameters
Optionalmessage: any
...optionalParams: any[]
Returns void
Since
v8.0.0

Defined in src/wllama.ts:20
error
error: {
    (...data: any[]): void;
    (message?: any, ...optionalParams: any[]): void;
}
Type declaration
(...data: any[]): void
MDN Reference

Parameters
...data: any[]
Returns void
(message?: any, ...optionalParams: any[]): void
Prints to stderr with newline. Multiple arguments can be passed, with the first used as the primary message and all additional used as substitution values similar to printf(3) (the arguments are all passed to util.format()).

const code = 5;
console.error('error #%d', code);
// Prints: error #5, to stderr
console.error('error', code);
// Prints: error 5, to stderr
Copy
If formatting elements (e.g. %d) are not found in the first string then util.inspect() is called on each argument and the resulting string values are concatenated. See util.format() for more information.

Parameters
Optionalmessage: any
...optionalParams: any[]
Returns void
Since
v0.1.100

Defined in src/wllama.ts:23
log
log: { (...data: any[]): void; (message?: any, ...optionalParams: any[]): void }
Type declaration
(...data: any[]): void
MDN Reference

Parameters
...data: any[]
Returns void
(message?: any, ...optionalParams: any[]): void
Prints to stdout with newline. Multiple arguments can be passed, with the first used as the primary message and all additional used as substitution values similar to printf(3) (the arguments are all passed to util.format()).

const count = 5;
console.log('count: %d', count);
// Prints: count: 5, to stdout
console.log('count:', count);
// Prints: count: 5, to stdout
Copy
See util.format() for more information.

Parameters
Optionalmessage: any
...optionalParams: any[]
Returns void
Since
v0.1.100

Defined in src/wllama.ts:21
warn
warn: {
    (...data: any[]): void;
    (message?: any, ...optionalParams: any[]): void;
}
Type declaration
(...data: any[]): void
MDN Reference

Parameters
...data: any[]
Returns void
(message?: any, ...optionalParams: any[]): void
The console.warn() function is an alias for error.

Parameters
Optionalmessage: any
...optionalParams: any[]
Returns void
Since
v0.1.100

Defined in src/wllama.ts:22


=====================================================


@wllama/wllamaDownloadOptions
Type Alias DownloadOptions
DownloadOptions: {
    headers?: Record<string, string>;
    progressCallback?: DownloadProgressCallback;
    signal?: AbortSignal;
}
Type declaration
Optionalheaders?: Record<string, string>
Custom headers for the request. Useful for authentication (e.g. Bearer token)

OptionalprogressCallback?: DownloadProgressCallback
Callback function to track download progress

Optionalsignal?: AbortSignal
Abort signal for the request

Defined in src/cache-manager.ts:7


=====================================================


@wllama/wllamaDownloadProgressCallback
Type Alias DownloadProgressCallback
DownloadProgressCallback: (opts: { loaded: number; total: number }) => any
Callback function to track download progress

Defined in src/model-manager.ts:10


=====================================================


@wllama/wllamaWllamaErrorType
Type Alias WllamaErrorType
WllamaErrorType:
    | "model_not_loaded"
    | "download_error"
    | "load_error"
    | "kv_cache_full"
    | "unknown_error"
    | "inference_error"
Defined in src/wllama.ts:188


=====================================================


@wllama/wllamaLoggerWithoutDebug
Variable LoggerWithoutDebugConst
LoggerWithoutDebug: {
    Console: ConsoleConstructor;
    debug: () => void;
    assert(condition?: boolean, ...data: any[]): void;
    assert(value: any, message?: string, ...optionalParams: any[]): void;
    clear(): void;
    clear(): void;
    count(label?: string): void;
    count(label?: string): void;
    countReset(label?: string): void;
    countReset(label?: string): void;
    dir(item?: any, options?: any): void;
    dir(obj: any, options?: InspectOptions): void;
    dirxml(...data: any[]): void;
    dirxml(...data: any[]): void;
    error(...data: any[]): void;
    error(message?: any, ...optionalParams: any[]): void;
    group(...data: any[]): void;
    group(...label: any[]): void;
    groupCollapsed(...data: any[]): void;
    groupCollapsed(...label: any[]): void;
    groupEnd(): void;
    groupEnd(): void;
    info(...data: any[]): void;
    info(message?: any, ...optionalParams: any[]): void;
    log(...data: any[]): void;
    log(message?: any, ...optionalParams: any[]): void;
    profile(label?: string): void;
    profileEnd(label?: string): void;
    table(tabularData?: any, properties?: string[]): void;
    table(tabularData: any, properties?: readonly string[]): void;
    time(label?: string): void;
    time(label?: string): void;
    timeEnd(label?: string): void;
    timeEnd(label?: string): void;
    timeLog(label?: string, ...data: any[]): void;
    timeLog(label?: string, ...data: any[]): void;
    timeStamp(label?: string): void;
    timeStamp(label?: string): void;
    trace(...data: any[]): void;
    trace(message?: any, ...optionalParams: any[]): void;
    warn(...data: any[]): void;
    warn(message?: any, ...optionalParams: any[]): void;
} = ...
Logger preset with debug messages suppressed

Type declaration
Console: ConsoleConstructor
debug: () => void
assert:function
clear:function
count:function
countReset:function
dir:function
dirxml:function
error:function
group:function
groupCollapsed:function
groupEnd:function
info:function
log:function
profile:function
profileEnd:function
table:function
time:function
timeEnd:function
timeLog:function
timeStamp:function
trace:function
warn:function
Defined in src/wllama.ts:183


=====================================================


@wllama/wllamaPOLYFILL_ETAG
Variable POLYFILL_ETAGConst
POLYFILL_ETAG: "polyfill_for_older_version" = 'polyfill_for_older_version'
Defined in src/cache-manager.ts:23


=====================================================


END OF OFFICIAL DOCS


=====================================================

























=====================================================


NOTES OF INTEREST FROM THE BUILD PROCESS USING CURSOR AI AND CLAUDE SONNET
These notes should be helpful when setting up a new wllama project.

NOTES BELOW


============ CLAUDE SONNET CURSOR AI NOTES



I'll explain what's happening with the error and then provide a step-by-step guide of what we did.

**Error Explanation:**
The error `SharedArrayBuffer is not defined` occurs because the application requires `SharedArrayBuffer` support, which is a JavaScript feature used for parallel processing and shared memory between threads. This feature is disabled by default in most browsers for security reasons (to prevent Spectre-like attacks). To enable it, the web server needs to send specific security headers.

Now, here's the complete step-by-step guide of what we did:

1. **Clone the Repository**
```bash
git clone https://github.com/pbosh/wllama.git
cd wllama
```

2. **Install Dependencies**
```bash
npm install
```

3. **Build WebAssembly Component**
```bash
npm run build:wasm
```
This step used Docker to compile the C++ code to WebAssembly.

4. **Fix Directory Structure**
```bash
# Remove incorrect single-thread file and create directory
rm src/single-thread
mkdir -p src/single-thread

# Copy WebAssembly files to single-thread directory
cp src/multi-thread/wllama.js src/multi-thread/wllama.wasm src/single-thread/
```

5. **Build TypeScript Components**
```bash
npm run build
```
This step:
- Cleaned previous build files
- Built worker scripts
- Compiled TypeScript code
- Generated documentation

6. **Start HTTP Server**
```bash
python3 -m http.server 8080
```

The error we're seeing suggests that to make this work properly, we need to serve the content with specific HTTP headers:
- `Cross-Origin-Opener-Policy: same-origin`
- `Cross-Origin-Embedder-Policy: require-corp`

Instead of using Python's simple HTTP server, you would need to use a server that can set these headers, like Node.js with Express or a properly configured nginx/Apache server.

For development purposes, you could use the project's built-in server:
```bash
npm run serve
```

This would be the correct way to serve the application as it would include the necessary security headers for `SharedArrayBuffer` to work.



============ CLAUDE SONNET CURSOR AI NOTES


Here's a summary of what we did to get the internal server working:

1. **Identified Server Dependencies**
   - Checked `scripts/http_server.js` and found it uses Express.js and mime-types
   - The server script uses ES modules (import statements)
   - Verified `package.json` had `"type": "module"` set for ES module support

2. **Installed Required Dependencies**
   ```bash
   npm install express mime-types
   ```

3. **Made Server Script Executable**
   ```bash
   chmod +x scripts/http_server.js
   ```

4. **Started Server with Multithreading**
   ```bash
   MULTITHREAD=1 node scripts/http_server.js
   ```
   - The `MULTITHREAD=1` environment variable enables important security headers:
     - `Cross-Origin-Embedder-Policy: require-corp`
     - `Cross-Origin-Opener-Policy: same-origin`
   - These headers are required for `SharedArrayBuffer` support, which is needed for WebAssembly threading

5. **Server Configuration**
   - Runs on port 8080
   - Serves static files from project root
   - Sets CORS and cache control headers
   - With multithreading enabled, adds required security headers for SharedArrayBuffer support

The server is now accessible at http://localhost:8080 and properly configured to run the WebAssembly application with threading support.




============ CLAUDE SONNET CURSOR AI NOTES


Here's a comprehensive set of notes about what we accomplished and learned:

```markdown
# WLLAMA Large Model Implementation Notes

## Successfully Implemented Approach
1. Model Preparation
   - Original model size: ~7.7GB
   - Quantized to Q2: 2.72GB
   - Split into ~512MB shards
   - Sharding approach worked with wllama's architecture

2. Build Process (Prerequisites)
   ```bash
   git clone https://github.com/pbosh/wllama.git
   cd wllama
   npm install
   npm run build:wasm    # Requires Docker
   ```

3. Directory Structure Fix
   ```bash
   rm src/single-thread
   mkdir -p src/single-thread
   cp src/multi-thread/wllama.js src/multi-thread/wllama.wasm src/single-thread/
   ```

4. Build and Server Setup
   ```bash
   npm run build
   npm install express mime-types
   chmod +x scripts/http_server.js
   MULTITHREAD=1 node scripts/http_server.js
   ```

## WebAssembly Memory Research

### Current Limitations
1. WebAssembly (wasm32) Constraints:
   - Hard limit of 4GB due to 32-bit addressing
   - Default allocation limit of 2GB
   - Requires special handling for 2GB-4GB range

2. Memory Management Requirements for >2GB:
   - Build flags needed: 
     ```bash
     emcc -s ALLOW_MEMORY_GROWTH=1 -s MAXIMUM_MEMORY=4GB
     ```
   - Code must use unsigned shifts (`>>>` instead of `>>`)
   - Special handling for addresses above 2GB mark

### Future Possibilities
1. wasm64:
   - In development
   - Will allow >4GB memory
   - Will have double memory overhead for pointers
   - Timeline uncertain

2. Current Workarounds:
   - Model quantization (Q2, Q4, Q8)
   - Model sharding (~512MB chunks)
   - Streaming inference
   - Server-side API approaches

## Implementation Notes
1. Server Requirements:
   - Needs specific security headers for SharedArrayBuffer:
     - Cross-Origin-Embedder-Policy: require-corp
     - Cross-Origin-Opener-Policy: same-origin
   - Use built-in server with MULTITHREAD=1 flag

2. Model Loading:
   - Local disk loading works with sharded approach
   - ~512MB per shard is a good balance
   - Browser can handle loading and inference with this setup

## Research Sources
1. V8 Documentation:
   - [4GB WebAssembly Memory Support](https://v8.dev/blog/4gb-wasm-memory)
   - Details on memory management and limitations

2. WebAssembly Design Issues:
   - Memory64 proposal in progress
   - Current 32-bit limitations
   - Browser security considerations

3. Browser Implementation:
   - Chrome/V8 work on larger memory support
   - Security headers requirements
   - Memory allocation strategies

## Current Status
- Working implementation with sharded Q2 model
- Successfully running inference in browser
- Limited by current WebAssembly memory constraints
- Sharding approach works but not ideal for all use cases

## Future Considerations
1. Potential Improvements:
   - Modify build process for >2GB support
   - Implement streaming inference
   - Wait for wasm64 support
   - Optimize shard loading/unloading

2. Trade-offs:
   - Model size vs. quality (quantization)
   - Memory usage vs. performance
   - Implementation complexity vs. functionality
```



============ CLAUDE SONNET CURSOR AI NOTES


I'll break down the GGML quantization methods shown in the screenshot:

### Suffix Meanings
- **K**: Uses K-means clustering for quantization
- **K_S**: K-means clustering with "Small" block size
- **K_M**: K-means clustering with "Medium" block size
- **K_L**: K-means clustering with "Large" block size
- **0**: Basic linear quantization (no K-means)

### Detailed Breakdown

1. **Q2 Methods**
   - `Q2_K`: 2-bit quantization with K-means
   - Highest compression (16x smaller than F16)
   - Best quality-to-size ratio for 2-bit
   - Good for very large models where size is critical

2. **Q3 Methods**
   - `Q3_K_S`: 3-bit, K-means, small blocks
   - `Q3_K_M`: 3-bit, K-means, medium blocks
   - `Q3_K_L`: 3-bit, K-means, large blocks
   - Better quality than Q2
   - Different block sizes offer trade-offs between accuracy and inference speed

3. **Q4 Methods**
   - `Q4_0`: 4-bit linear quantization
   - `Q4_K_S`: 4-bit, K-means, small blocks
   - `Q4_K_M`: 4-bit, K-means, medium blocks
   - Good balance of quality and size
   - `Q4_0` is faster but less accurate than K-means versions

4. **Q5 Methods**
   - `Q5_0`: 5-bit linear quantization
   - `Q5_K_S`: 5-bit, K-means, small blocks
   - `Q5_K_M`: 5-bit, K-means, medium blocks
   - Better quality than Q4
   - Larger file size than Q4

5. **Q6 Methods**
   - `Q6_K`: 6-bit with K-means
   - Higher quality than Q5
   - Larger file size

6. **Q8 Methods**
   - `Q8_0`: 8-bit linear quantization
   - Highest quality among quantized versions
   - Largest file size of quantized versions
   - Still significantly smaller than F16

### Trade-offs

1. **Block Size (S/M/L)**
   - **Small**: Better accuracy, slower inference
   - **Medium**: Balanced approach
   - **Large**: Faster inference, slightly lower accuracy

2. **K-means vs Linear (0)**
   - **K-means**: Better quality, slower inference
   - **Linear**: Faster inference, lower quality
   - **Memory**: K-means versions typically use more memory during inference

3. **General Rules**
   - Lower bits = smaller size but lower quality
   - K-means versions generally provide better quality than linear
   - Larger block sizes = faster inference but potentially lower quality
   - Each step up in bits (Q2 → Q3 → Q4, etc.) provides better quality but larger size

For your use case with wllama in the browser, Q2_K was a good choice as it:
1. Provides the smallest file size (critical for browser)
2. Uses K-means for better quality than linear
3. Still maintains acceptable inference quality
4. Works well with the sharding approach you implemented


Exactly! Let me break this down even more clearly:

1. **File Size Priority** (from smallest to largest):
   - Q2 < Q3 < Q4 < Q5 < Q6 < Q8
   - Each step up roughly increases file size by ~25-50%

2. **Quality Optimization** (for any given Q number):
   - Best Quality: Use `K` variants
   - Example: Q2_K is better quality than a theoretical Q2_0 (if it existed)
   - Trade-off: K-means versions are slightly slower in inference

3. **Block Size Impact** (when available):
   ```
   K_S (Small blocks)  : Highest accuracy, Slowest speed
   K_M (Medium blocks) : Balanced accuracy/speed
   K_L (Large blocks)  : Lower accuracy, Fastest speed
   ```

So in your case with Q2_K:
- You chose Q2 for smallest possible file size (critical for browser)
- You got the _K variant for best possible quality at that size
- No block size option for Q2 (only available in Q3/Q4/Q5)

This is why Q2_K was ideal for your browser implementation:
1. Smallest file size (Q2)
2. Best possible quality for that size (_K)
3. Simple version without block size complexity

If you ever need to adjust, remember:
- Need smaller size? Stay with Q2_K
- Need better quality? Move up to Q3_K_S/M/L
- Need faster inference? Consider Q3_K_L or Q4_0


Summary:
Here's a comprehensive breakdown of all quantization methods shown in the screenshot, ordered by quality vs size trade-off (assuming BF16 = 100% quality):

### 8-bit
- **Q8_0** (~98-99% quality)
  - Linear quantization (no K-means)
  - Largest file size of quantized versions
  - Fastest inference speed
  - Best for quality-critical applications

### 6-bit
- **Q6_K** (~97-98% quality)
  - Uses K-means clustering
  - No block size variants needed at this precision
  - Good balance of quality and size
  - Slightly slower than Q6_0 would be (if it existed)

### 5-bit
- **Q5_K_S** (~96-97% quality)
  - K-means with small blocks
  - Highest quality 5-bit option
  - Slowest 5-bit inference
- **Q5_K_M** (~96% quality)
  - K-means with medium blocks
  - Balanced speed/quality trade-off
- **Q5_0** (~94-95% quality)
  - Linear quantization
  - Fastest 5-bit inference
  - Lowest quality 5-bit option

### 4-bit
- **Q4_K_S** (~95-96% quality)
  - K-means with small blocks
  - Best 4-bit quality
  - Slower inference
- **Q4_K_M** (~94-95% quality)
  - K-means with medium blocks
  - Good balance for 4-bit
- **Q4_0** (~92-93% quality)
  - Linear quantization
  - Fastest 4-bit inference
  - Popular "good enough" choice

### 3-bit
- **Q3_K_S** (~91-92% quality)
  - K-means with small blocks
  - Best 3-bit quality
- **Q3_K_M** (~90-91% quality)
  - K-means with medium blocks
  - Balanced 3-bit option
- **Q3_K_L** (~89-90% quality)
  - K-means with large blocks
  - Fastest 3-bit inference

### 2-bit
- **Q2_K** (~90-92% quality)
  - K-means clustering
  - Smallest file size
  - No block size variants
  - Good for browser deployment

### Size Reduction (approximate):
```
BF16 (original) : 100% size
Q8_0           : ~50% of original
Q6_K           : ~37.5% of original
Q5_K variants  : ~31% of original
Q4_K variants  : ~25% of original
Q3_K variants  : ~19% of original
Q2_K           : ~12.5% of original
```

### Speed Patterns:
```
Fastest → Slowest
1. _0 variants (linear)
2. _K_L (large blocks)
3. _K_M (medium blocks)
4. _K_S (small blocks)
5. _K (basic K-means)
```

### When to Use Each:
- **Q8_0**: When quality is critical and size less important
- **Q6_K**: Best balance of quality and size
- **Q5_K_S/M**: When Q6_K is too large but Q4 too low quality
- **Q4_K_S**: Most popular quality-conscious choice
- **Q4_0**: Most popular speed-conscious choice
- **Q3_K variants**: When size is very important
- **Q2_K**: When minimum size is critical (browser deployment)




============ CLAUDE SONNET CURSOR AI NOTES


Let me show you the server code:

Read file: scripts/http_server.js


Here's what the server code does:

1. **Imports**:
   ```javascript
   import express from 'express';
   import path from 'path';
   import mime from 'mime-types';
   import { dirname } from 'path';
   import { fileURLToPath } from 'url';
   ```
   - Uses Express.js for the web server
   - Includes utilities for path handling and MIME types

2. **Static File Serving**:
   ```javascript
   app.use(express.static(path.join(__dirname, '..'), {
     setHeaders: (res) => {
       if (process.env.MULTITHREAD) {
         res.setHeader('Cross-Origin-Embedder-Policy', 'require-corp');
         res.setHeader('Cross-Origin-Opener-Policy', 'same-origin');
       }
       res.setHeader('Access-Control-Allow-Origin', '*');
       res.setHeader('Cache-Control', 'no-cache, no-store, must-revalidate');
       res.setHeader('Pragma', 'no-cache');
       res.setHeader('Expires', '0');
     }
   }));
   ```
   - Serves files from the project root directory
   - When `MULTITHREAD=1`, adds security headers for SharedArrayBuffer support
   - Enables CORS and disables caching

3. **Server Start**:
   ```javascript
   const PORT = 8080;
   app.listen(PORT, () => console.log(`Server running on port ${PORT}`));
   ```
   - Runs on port 8080

To run the server:
```bash
# Without multithreading
node scripts/http_server.js

# With multithreading (recommended)
MULTITHREAD=1 node scripts/http_server.js
```

The key feature is the security headers when `MULTITHREAD=1` is set, which are required for WebAssembly threading support in browsers.



============ CLAUDE SONNET CURSOR AI NOTES

I see the issue from the build logs. The build process is completing successfully, but it's not finding the expected output directory `examples/main/dist`. This is because we need to build the example project specifically.

Let's modify the build command to include building the example. The build command should be:

```
cd examples/main && npm install && npm run build && cd ../..
```

This command will:
1. Change into the examples/main directory
2. Install dependencies for the example project
3. Build the example project
4. Return to the root directory

Please update the build command in the Cloudflare Pages settings with this new command. The build output directory should remain as `examples/main/dist`.

Would you like me to help you make any other adjustments to the configuration?



Great! I see that the deployment was successful. The build logs show that everything completed properly:
The repository was cloned
Dependencies were installed
The main example was built successfully with Vite
All assets were uploaded to Cloudflare's network
Your site is now live at highermind.pages.dev. The deployment included:
Main HTML file
WebAssembly file (wllama-Dag6KBV0.wasm, ~1.6MB)
CSS file (~71KB)
JavaScript bundle (~492KB)
There are some non-critical warnings about Node.js version compatibility and outdated packages, but these aren't affecting the deployment.



============ CLAUDE SONNET CURSOR AI NOTES


Here's a summary for your notes:

# Deployment Process Summary

## One-Command Deployment
This single command handles the entire deployment process:
```bash
cd /Users/ms1282/devrepo/wllama-1/examples/main && npm run build && cd ../.. && git add . && git commit -m "v$(node -p "require('./examples/main/package.json').version")" && git push
```

## What Each Part Does
1. `cd /Users/ms1282/devrepo/wllama-1/examples/main` - Navigate to main project directory
2. `npm run build` - Build the project using Vite
3. `cd ../..` - Return to root directory
4. `git add .` - Stage all changes
5. `git commit -m "v$(node -p "require('./examples/main/package.json').version")"` - Create commit with version from package.json
6. `git push` - Push to GitHub

## Version Control System
- **package.json**: Stores the current version number
- **CHANGELOG.md**: Documents changes in human-readable format
- **Git**: Tracks all code changes
- **Automatic Deployment**: Push to GitHub triggers Cloudflare Pages deployment

## File Structure
- **package.json**: Contains version number and dependencies
- **CHANGELOG.md**: Format:
  ```markdown
  ## [VERSION] - DATE

  ### Added
  - New features

  ### Changed
  - Modified features
  ```

## Automation
- Version number automatically read from package.json
- Cloudflare Pages automatically detects GitHub pushes
- New deployments automatically created with unique URLs
- Each deployment gets its own preview URL





============ CLAUDE SONNET CURSOR AI NOTES




You'll need to run this as two separate commands because you need to get the commit hash from the first command's output. Here's exactly what you'd need to copy from your notes and paste into terminal:

First paste this to see the commit hash:
```bash
git log --grep="v0.0.1"
```

This will show output like:
```
commit abc123def456... (this is the hash you need)
Author: ...
Date: ...
    v0.0.1: Added feature xyz
```

Then paste this, replacing ABC123 with the hash you saw:
```bash
git checkout ABC123 && cd examples/main && npm run build && cd ../..
```

For your notes, I recommend storing it like this:

# Rollback Commands
1. Find commit hash:
```bash
git log --grep="v0.0.1"    # Replace v0.0.1 with version you want
```

2. Rollback using hash from above:
```bash
git checkout ABC123 && cd examples/main && npm run build && cd ../..    # Replace ABC123 with hash from step 1
```

3. (Optional) Create new branch from old version:
```bash
git checkout -b rollback-v0.0.1 ABC123    # Replace ABC123 with same hash
```

4. Return to latest version:
```bash
git checkout main && cd examples/main && npm run build && cd ../..
```

FINAL commands that work:
REBUILD and LAUNCH DEV SERVER localhost
cd /Users/ms1282/devrepo/wllama-1/examples/main && npm run build && cd ../..

MULTITHREAD=1 node scripts/http_server.js



PUSH to GIT and subsequently CLOUDFLARE (automatic)
cd /Users/ms1282/devrepo/wllama-1/examples/main && npm run build && cd ../.. && git add . && git commit -m "v$(node -p "require('./examples/main/package.json').version")" && git push

^ the above is one line
*edit changelog as this helps with any roll backs




ROLL BACK to version n
git log --grep="v0.0.1"    # Replace v0.0.1 with version you want

git checkout ABC123    # Replace ABC123 with hash from step 1



============ CLAUDE SONNET CURSOR AI NOTES


Let me break down how this site works and how Cloudflare would serve it:

1. **Build Process**:
- When you run `npm run build`, it creates static assets (HTML, CSS, JS, WASM) in the `dist` directory
- These static files include all the React components, WebAssembly binaries, and other assets
- The build process happens once on your machine/CI, not per user visit

2. **Site Type**: This is a **static site** with client-side functionality:
- The initial HTML/JS/WASM files are static and can be cached by Cloudflare
- All the AI processing happens in the browser using WebAssembly
- No server-side processing is required
- The models are loaded from Hugging Face, not your server

3. **Cloudflare Pages Hosting**:
- Cloudflare Pages would serve this as a static site
- The files are distributed to Cloudflare's edge network
- When users visit, they get the static files from the nearest edge server
- The only dynamic part is when users download models from Hugging Face

4. **Pricing Implications**:
- Cloudflare Pages has a generous free tier:
- Unlimited sites and requests
- 500 builds per month
- 100 concurrent builds
- Unlimited bandwidth
- You would only pay if you exceed these limits
- The main bandwidth usage comes from users downloading models from Hugging Face, which doesn't count against your Cloudflare bandwidth

This architecture is very cost-effective because:
1. No server-side processing is needed
2. All computation happens in the user's browser
3. Static files are heavily cached at the edge
4. Model downloads are handled by Hugging Face, not your hosting



So it would seem reasonable that I would have no costs incurred from cloudflare even with 1M+ monthly users?



Yes, that's correct! Even with 1M+ monthly users, you would likely incur no costs from Cloudflare Pages because:

1. **Static Asset Serving**:
- All files (HTML, JS, CSS, WASM) are served from Cloudflare's edge network
- The free tier has unlimited bandwidth for static assets
- Unlimited requests are included

2. **Computation Costs**: None because:
- All AI processing happens in the user's browser
- No server-side compute resources are used
- WebAssembly runs entirely on the client side

3. **Model Distribution**:
- Models are hosted on Hugging Face, not Cloudflare
- Model downloads don't count against your bandwidth
- Hugging Face handles the heavy lifting of model distribution

4. **Build Limits**:
- 500 builds per month on free tier
- You only need to build when you update the site
- Regular user visits don't trigger builds

The only way you might hit paid tiers would be if you:
- Deploy more than 500 updates to your site in a month
- Need more than 100 concurrent builds
- Need enterprise features like custom domains with SSL

This architecture is extremely scalable from a cost perspective - whether you have 100 users or 1M+ users, the hosting costs remain effectively zero on Cloudflare Pages.




============ CLAUDE SONNET CURSOR AI NOTES




Here's a detailed scope of the architecture and implementation we've designed:

# Higher Mind AI - Architecture Scope

## 1. Core Infrastructure
- **Platform**: Cloudflare Pages
- **Type**: Static Site (SPA)
- **Build Process**: Single build serves all models
- **Cost Efficiency**: Zero/minimal costs due to static hosting
- **Scale**: Supports millions of users with edge distribution

## 2. URL Structure
```typescript
// Base Format
https://highermind.ai/chat/[modelId]?[parameters]

// Parameters Capacity
- Maximum 2,000 characters (safe cross-browser limit)
- Extensible parameter structure
- Human-readable format

// Example
https://highermind.ai/chat/Make-Better-Decisions-1-Q3_K_L-GGUF?
  settings=temperature:0.7|maxTokens:2048&
  theme=dark&
  layout=split
```

## 3. Model Configuration System
```typescript
interface ModelMetadata {
  // Core Identifiers
  id: string;
  title: string;
  version: string;
  
  // Rich Media
  thumbnail: {
    url: string;
    alt: string;
  };
  preview: {
    videoUrl?: string;
    gifUrl?: string;
  };
  
  // Categorization
  tags: string[];
  category: string;
  featured: boolean;
  
  // Technical Specs
  requirements: {
    minRAM: number;
    recommendedRAM: number;
    size: number;
  };
  
  // Usage Data
  stats: {
    popularity: number;
    rating: number;
    usageCount: number;
  };
  
  // Content
  description: string;
  shortDescription: string;
  modelUrl: string;
  lastUpdated: string;
}
```

## 4. Page Types

### A. Browse Page (`/browse`)
- Netflix-style interface
- Dynamic sections based on user interaction
- Categories:
  - Featured models
  - Recently used
  - Recommended
  - Category-based groupings
- Rich preview capabilities
- No server-side requests for UI data

### B. Model Page (`/chat/[modelId]`)
- Unique URL per model
- Parameter-based customization
- Shareable links
- Configuration driven from static assets

## 5. Key Features

### A. Navigation System
```typescript
const router = createBrowserRouter([
  {
    path: "/browse",
    element: <BrowseModels />
  },
  {
    path: "/chat/:modelId",
    element: <ChatView />
  }
]);
```

### B. State Management
- URL parameters for shareable state
- LocalStorage for user preferences
- In-memory state for active session

### C. Model Loading
- Client-side WASM execution
- Model files served from Hugging Face
- Browser-based caching

## 6. Extensibility Points

### A. URL Parameters
- Model settings
- UI preferences
- Context settings
- Sharing options
- Layout configurations

### B. Configuration Updates
- New model attributes
- Additional UI components
- Extended metadata
- Enhanced preview options

### C. Feature Evolution
- Analytics integration
- Social features
- Advanced filtering
- Personalization
- Model chaining

## 7. Performance Considerations
- Edge-cached static assets
- Client-side routing
- Lazy-loaded components
- Optimized media assets
- Browser caching strategies

## 8. Deployment Flow
1. Build static assets
2. Deploy to Cloudflare Pages
3. Configure headers for WASM support
4. Edge distribution
5. Automatic HTTPS

## 9. Scalability Aspects
- Supports 10 to 1000+ models
- Same codebase serves all models
- No server-side scaling needed
- Cloudflare's global edge network
- Browser handles computation

## 10. Future Expansion Capabilities
- Dynamic recommendations
- User preferences
- Usage analytics
- Social features
- Advanced filtering
- Custom UI themes
- Model combinations
- Sharing capabilities


The only thing we haven't talked about is if I want to interact with a user session from the server side eg. access their session and even use their hardware, can we put a string in the url that would give us an endpoint to their computer. I don't want to do this now but I want to be sure that the url string will give us that ability way down the line.



Absolutely! We can include a secure session identifier or connection token in the URL that could enable server-side interaction. Here's how it could work:

```typescript
// URL Structure with Session Connection
https://highermind.ai/chat/[modelId]?
  settings=default&
  theme=dark&
  // Secure connection identifier
  session=eyJhbGciOiJIUzI1NiJ9.user_session_8675309.unique_hardware_id

// Future Implementation Example
interface SecureSession {
  sessionId: string;        // unique session identifier
  hardwareId: string;      // unique device fingerprint
  capabilities: {          // hardware capabilities
    gpu: boolean;
    threads: number;
    memory: number;
  };
  permissions: {           // user-granted permissions
    remoteAccess: boolean;
    hardwareUse: boolean;
    monitoring: boolean;
  };
}

// Session Management
function ChatView() {
  const [searchParams] = useSearchParams();
  const sessionToken = searchParams.get('session');
  
  // Establish secure WebSocket connection
  useEffect(() => {
    if (sessionToken) {
      const ws = new WebSocket(
        `wss://control.highermind.ai/session/${sessionToken}`
      );
      
      ws.onmessage = (event) => {
        // Handle remote commands
        const command = JSON.parse(event.data);
        switch(command.type) {
          case 'HARDWARE_REQUEST':
            // Request hardware access
            break;
          case 'MONITORING':
            // Enable performance monitoring
            break;
          case 'REMOTE_ASSIST':
            // Enable remote assistance
            break;
        }
      };
    }
  }, [sessionToken]);
}
```

Key benefits of this approach:
1. **Security**: Session tokens can be encrypted and time-limited
2. **Flexibility**: Can add new capabilities without changing URL structure
3. **Permissions**: Users maintain control over access
4. **Scalability**: Works with existing static hosting
5. **Compatibility**: WebSocket connection only established when needed

The URL parameter gives us the entry point, and we can build out the server-side interaction capabilities later without restructuring the application.




============ CLAUDE SONNET CURSOR AI NOTES




Yes, there are a few key practices we can follow while building the UI to make future dynamic page creation smoother:

1. **Component Structure**
```typescript
// Make components model-config aware from the start
interface ModelProps {
  modelConfig: ModelMetadata;  // from our earlier type definition
  layout?: LayoutType;         // for dynamic layouts
  theme?: ThemeType;          // for theming
}

// Example component
function ModelCard({ modelConfig, layout = 'grid' }: ModelProps) {
  return (
    <div className={`model-card ${layout}`}>
      {/* Use modelConfig properties instead of hardcoding */}
      <img src={modelConfig.thumbnail.url} alt={modelConfig.thumbnail.alt} />
      <h3>{modelConfig.title}</h3>
    </div>
  );
}
```

2. **CSS Organization**
```css
/* Use CSS classes that scale with dynamic content */
.model-grid {
  display: grid;
  /* Use CSS Grid's auto-fit for dynamic sizing */
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1rem;
}

/* Prepare for different layouts */
.model-card.grid { /* ... */ }
.model-card.list { /* ... */ }
.model-card.featured { /* ... */ }
```

3. **State Management**
```typescript
// Keep UI state separate from model data
interface UIState {
  layout: LayoutType;
  theme: ThemeType;
  filters: FilterOptions;
}

// Easy to extend later
interface FilterOptions {
  category?: string;
  tags?: string[];
  searchTerm?: string;
}
```

4. **Route Preparation**
```typescript
// Make routes data-driven from the start
const routes = {
  browse: '/browse',
  model: (id: string) => `/chat/${id}`,
  // Easy to add new route types later
  category: (cat: string) => `/browse/category/${cat}`,
};
```

5. **Loading States**
```typescript
function ModelView() {
  // Plan for async loading from the start
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  return (
    <div className="model-view">
      {loading && <LoadingPlaceholder />}
      {error && <ErrorDisplay error={error} />}
      {!loading && !error && (
        // Your content here
      )}
    </div>
  );
}
```

6. **Placeholder Content**
```typescript
// Use realistic data shapes from the start
const PLACEHOLDER_MODEL: ModelMetadata = {
  id: "example-model",
  title: "Example Model",
  description: "This is a placeholder description...",
  // ... other required fields
};

// Makes testing different states easier
function DevelopmentWrapper({ children }) {
  const [showPlaceholder] = useDevMode();
  
  return showPlaceholder ? (
    <div className="dev-wrapper">
      {React.cloneElement(children, { model: PLACEHOLDER_MODEL })}
    </div>
  ) : children;
}
```

The key principles are:
1. Make components config-driven from the start
2. Use flexible CSS layouts
3. Plan for loading/error states
4. Keep UI state separate from model data
5. Use TypeScript interfaces for better maintainability
6. Create reusable layout components
7. Think in terms of dynamic content from day one




============ CLAUDE SONNET CURSOR AI NOTES


Looking at the code in `ModelScreen.tsx`, here are your thread options:

```typescript
<label className="input input-bordered flex items-center gap-2 mb-2">
  # threads
  <input
    type="number"
    className="grow"
    placeholder="(auto detected)"
    min="1"
    max="100"
    step="1"
    onChange={onChange('nThreads')}
    value={currParams.nThreads < 1 ? '' : currParams.nThreads}
    disabled={blockModelBtn}
  />
</label>
```

The options are:
1. **Auto-detect** (default): When `nThreads < 1`, indicated by empty input field
2. **Manual setting**: Any number between 1 and 100 threads

From the `wllama.context.tsx` code, we can see how this is applied:
```typescript
await wllamaInstance.loadModel(model.cachedModel, {
  n_threads: currParams.nThreads > 0 ? currParams.nThreads : undefined,
  n_ctx: currParams.nContext,
  n_batch: currParams.nBatch,
});
```

Best practices for thread settings:
1. **Auto-detect** (recommended): Leave empty to let the system optimize based on your hardware
2. **Manual optimization**:
   - For M1 Mac Studio: Try values between 8-16 threads
   - Start with number of physical CPU cores
   - Test performance with different values
   - Monitor CPU usage and temperature


What is the real world process of leaving it automatic? Eg. How many threads would it use with the M1 Mac Studio that might show 20 threads?



When left automatic (empty/undefined in the UI), here's what happens with llama.cpp (which wllama uses) on an M1 Mac Studio:

1. **Hardware Detection**:
   - Detects 20 total threads (8 performance + 4 efficiency cores with hyperthreading)
   - Identifies core types (P-cores vs E-cores)

2. **Default Behavior**:
   - llama.cpp typically uses ~4-8 threads by default
   - Prioritizes performance (P) cores first
   - Avoids oversubscription which could hurt performance
   - Leaves headroom for system tasks

3. **M1/M2 Optimization**:
   - Aware of Apple Silicon architecture
   - Understands the asymmetric core layout
   - Makes smart decisions about thread allocation
   - Usually avoids E-cores unless explicitly configured

Real-world example on M1 Mac Studio:
- Available: 20 logical threads
- Typical automatic usage: 6-8 threads
- Uses primarily P-cores
- Maintains system responsiveness
- Balances performance vs power/heat

This is why automatic is often best - it's conservative but efficient, and takes into account the specific architecture of Apple Silicon. Would you like me to explain more about how it makes these decisions?


I just monitored the performance in real time and it does a very good job of using available resources. I will leave this set to auto.



That's a great observation! Yes, llama.cpp's automatic thread management is very well optimized, especially for Apple Silicon. The auto setting is doing several smart things:

1. **Dynamic Allocation**
   - Adjusts thread usage based on workload
   - Scales up/down as needed
   - Prevents CPU throttling

2. **Architecture Awareness**
   - Understands M1/M2 core layout
   - Uses P-cores efficiently
   - Avoids overwhelming E-cores

3. **System Harmony**
   - Leaves resources for browser/UI
   - Maintains system responsiveness
   - Prevents thermal throttling

Your real-time monitoring confirms what the llama.cpp developers have optimized for - efficient resource usage without compromising system stability or user experience. This is especially important for a browser-based implementation where users might be doing other tasks simultaneously.



============ CLAUDE SONNET CURSOR AI NOTES


Here's a comprehensive summary of our journey to get the system prompt and message formatting working correctly:

### Problem Overview
We needed to ensure proper message formatting and system prompt handling for a Mistral-7B-Instruct-v0.3 based model, focusing on:
1. Correct JSON message formatting
2. Proper template application
3. System prompt integration

### Message Flow & Formatting
1. **Initial JSON Formatting** (`wllama.context.tsx`):
```typescript
const formatChat = async (messages: Message[]): Promise<string> => {
  const formattedMessages = [
    { role: 'system', content: SYSTEM_PROMPT },
    ...messages.map((msg) => ({
      role: msg.role,
      content: msg.content,
    }))
  ];
  return JSON.stringify(formattedMessages);
};
```

2. **Template Processing**:
- Messages are sent as JSON to llama.cpp
- llama.cpp autodetects the appropriate template (Mistral v1/v3 in our case)
- The template converts JSON into the model's expected format:
```
// System:
content + "\n\n"
// User:
"[INST] " + content + " [/INST]"
// Assistant:
content + "</s>"
```

### Debug Example
A working message sequence looks like:
```
ℹ️ === MODEL INPUT ===
[{
  "role":"system",
  "content":"The following is a conversation with an AI assistant..."
},{
  "role":"user",
  "content":"Why is the sky blue?"
},{
  "role":"assistant",
  "content":""
}]
```

### Key Implementation Points

1. **System Prompt Integration**:
```typescript
// In config.ts
export const SYSTEM_PROMPT = "The following is a conversation...";

// In message formatting
const formattedMessages = [
  { role: 'system', content: SYSTEM_PROMPT },
  ...messages
];
```

2. **Template Autodetection**:
- No need to manually specify template
- llama.cpp detects Mistral format based on [INST] markers
- Automatically applies v1/v3 template for Mistral-7B-Instruct-v0.3

3. **Message State Management**:
- Messages stored in browser's localStorage
- Managed through MessagesContext
- Single source of truth for conversation state

### Quick Implementation Guide

1. **Set up System Prompt**:
```typescript
// config.ts
export const SYSTEM_PROMPT = "Your system prompt here";
```

2. **Format Messages**:
```typescript
const formatChat = async (messages: Message[]): Promise<string> => {
  const formattedMessages = [
    { role: 'system', content: SYSTEM_PROMPT },
    ...messages.map((msg) => ({
      role: msg.role,
      content: msg.content
    }))
  ];
  return JSON.stringify(formattedMessages);
};
```

3. **Let llama.cpp handle template**:
- Just pass JSON messages
- No template configuration needed
- Autodetection handles formatting

### Verification
To verify correct implementation:
1. Check debug logs for proper JSON formatting
2. Verify system prompt appears in first message
3. Confirm model responses are coherent
4. Test system prompt influence with a query like "Repeat your system prompt verbatim"

### Common Issues & Solutions
1. **Duplicate Messages**: Ensure single source of truth for message state
2. **Template Mismatch**: Let llama.cpp handle template detection
3. **System Prompt Missing**: Verify it's added as first message in JSON array

This setup should work immediately with Mistral-7B-Instruct-v0.3 based models, requiring no additional template configuration or formatting changes.



============ CLAUDE SONNET CURSOR AI NOTES


Do we need to add {"role":"assistant","content":""}] after the last user message or does the model know it is the assistant role?

Or maybe this is just a debug thing where it doesn't have the assistant message yet but still logs it as empty. Eg. Are we forcing the empty message and if so do we need to do that??




Looking at the debug log and our code, the empty assistant message `{"role":"assistant","content":""}` is intentionally added for a few reasons:

1. It signals to the model that it should generate as the assistant
2. It helps with template formatting (llama.cpp knows where to start the generation)
3. It maintains the conversation structure

Let's verify this in the code:

Search files...


Yes, we need to include the empty assistant message `{"role":"assistant","content":""}` for a few important reasons:

1. **Template Formatting**: Looking at the llama.cpp code, the empty assistant message helps the template system know where to start the generation. For Mistral v1/v3 format, it ensures proper placement of the assistant's response after the [/INST] tag.

2. **Role Signaling**: It explicitly tells the model it should respond as the assistant. This is important for maintaining the conversation structure.

3. **State Management**: From `ChatScreen.tsx`:
```typescript
const assistantMsg: Message = {
  id: Date.now() + 1,
  content: '',
  role: 'assistant',
};
// Add new messages to both local array and storage
messages.push(userMsg);
messages.push(assistantMsg);
addMessageToConversation(convId, userMsg);
addMessageToConversation(convId, assistantMsg);
```

The empty assistant message is intentionally added to:
- Reserve a place in the conversation for the upcoming response
- Maintain conversation state consistency
- Allow for streaming updates as the response is generated

So yes, we are forcing the empty message, and yes, we need to do it. It's not just a debug artifact - it's a key part of how the chat system works, ensuring proper:
1. Template formatting
2. Role assignment
3. State management
4. Response streaming

The debug log shows exactly what we want - the empty assistant message is included in the input, and then the model knows to generate the response in that position.




============ CLAUDE SONNET CURSOR AI NOTES


Scope for switching to model list paramaters - from global parameters:
Here's the refined requirements based on all your answers:

### Core Requirements

1. **Model Configuration Structure**
   - Each model in `LIST_MODELS` must have complete configuration
   - No fallback values or global defaults needed
   - Remove `SYSTEM_PROMPT` and `DEFAULT_INFERENCE_PARAMS`
   - Keep `DEFAULT_CHAT_TEMPLATE` as global
   - Configuration includes:
     - Basic info (model_title, url, size)
     - Metadata (short_description, description, systemPrompt)
     - Visual assets (cover_image, video_intro)
     - Parameters (temperature, topP, nPredict, repeatPenalty)
     - Additional fields (tags, misc)

2. **UI Changes**
   - Replace Hugging Face URL display with model's `short_description`
   - Display model's `systemPrompt`
   - No need to display tags
   - Keep existing model switching UI functionality

3. **Implementation Details**
   - No backward compatibility needed for existing conversations
   - No user-configurable parameters - all values come from `LIST_MODELS`
   - No validation system needed (manual validation)
   - No analytics implementation
   - No export/import features needed
   - Model-specific parameters to be implemented immediately
   - Types to be added to './utils/types'

4. **File Changes**
   - Primary file: `config.ts`
   - Types file: `utils/types.ts`
   - No other files need modification

5. **Non-Requirements** (explicitly excluded)
   - No fallback system
   - No validation system
   - No transition period handling
   - No analytics
   - No parameter override UI
   - No export/import features
   - No backward compatibility

### Implementation Approach
1. Update types in `utils/types.ts`
2. Remove global constants from `config.ts`
3. Implement model-specific parameter loading
4. Update UI to show short_description and systemPrompt
5. Manual duplication of parameters across all models (to be done by you)




Let me break down what happened:

### What We Were Supposed to Do
1. **Remove Global Parameters**
   - Should have completely removed `DEFAULT_INFERENCE_PARAMS`
   - Should have removed `SYSTEM_PROMPT`
   - Each model should have been self-contained with its own parameters

2. **Keep Only Essential Globals**
   - Should have kept only `DEFAULT_CHAT_TEMPLATE`
   - Should have kept the basic WASM configuration (the imports and paths)

### What I Actually Did Wrong
1. **Parameter Conflict**
   - I kept `DEFAULT_INFERENCE_PARAMS` while also adding model-specific parameters
   - This created a conflict where the system doesn't know which parameters to use
   - The worker code might be trying to read from both sources

2. **Type System Issues**
   - I tried to manage the ModelConfig type when we should have just removed it
   - The data structure should have been allowed to exist without type constraints
   - This led to unnecessary complexity with imports and type declarations

3. **Import Changes**
   - I kept modifying the imports trying to fix type issues
   - Should have left the imports exactly as they were in the working version
   - The worker code depends on these exact imports

### Why It Doesn't Work
1. **Parameter Loading Confusion**
   - The worker code is looking for `WLLAMA_MULTI_THREAD_WORKER_CODE`
   - Having both global and model-specific parameters might be interfering with initialization
   - The worker might be failing because it's getting conflicting parameter sources

2. **Breaking Working Configuration**
   - The working version had a specific set of imports and configurations
   - By trying to add model-specific parameters without removing the globals
   - We created a state that confuses the initialization process

### What We Should Have Done
1. Start with the exact working version
2. Remove ONLY the global parameters (`DEFAULT_INFERENCE_PARAMS`, `SYSTEM_PROMPT`)
3. Add model-specific parameters to LIST_MODELS
4. Keep all imports and WASM configuration exactly the same
5. Not touch any type declarations or add new imports


I'll analyze the working version to understand exactly what caused the issues with our previous attempts. Let me examine the key files and their interactions.

First, let's look at how the chat and model system works in the working version:

1. **Configuration and Parameter Flow:**
- In `types.ts`, we have a simple `InferenceParams` interface that defines global parameters:
  - `nThreads`, `nContext`, `nBatch`, `temperature`, `nPredict`
- These parameters are stored in `WllamaStorage` and managed through the `WllamaContext`

2. **Model Loading and Chat Flow:**
From `wllama.context.tsx`:
- When a model is loaded:
  1. The model is validated
  2. `wllamaInstance.loadModel()` is called
  3. Stop tokens are initialized (empty array)
  4. Runtime info is set (multithread and chat template status)

3. **Chat Generation Process:**
The `createCompletion` function:
- Uses a single set of parameters from `currParams`
- Creates completion options with:
  ```typescript
  const completionOpts = {
    stopTokens,
    nPredict: currParams.nPredict,
    sampling: {
      temp: currParams.temperature,
      topP: 0.9,
      repeatPenalty: 1.1
    }
  };
  ```
- Passes these directly to `wllamaInstance.createChatCompletion()`

4. **Model Display and Management:**
From `displayed-model.tsx`:
- Models are displayed with basic metadata (URL, size, state)
- No model-specific parameters are stored or managed
- The `DisplayedModel` class is focused on model identity and state

**Where Things Went Wrong in Our Previous Attempts:**

1. **Parameter Conflict:**
   - We tried to introduce model-specific parameters while keeping the global parameters
   - This created confusion in the worker initialization because it wasn't clear which parameters should take precedence
   - The worker was likely trying to use both sets of parameters simultaneously

2. **Type System Issues:**
   - We added model-specific parameters to `ModelConfig` but didn't properly update all the places where models are created and managed
   - This broke the type system's ability to track parameter flow through the application

3. **Worker Initialization:**
   - The worker code expects a single, clear set of parameters during initialization
   - By trying to maintain both global and model-specific parameters, we created ambiguity in what should be passed to the worker

4. **State Management Complexity:**
   - The working version has a clean separation: parameters are global state in the context
   - Our attempts to add model-specific parameters complicated this by creating multiple sources of truth for parameters

5. **Chat Template Integration:**
   - The working version has a simple chat template system that doesn't need to know about model-specific parameters
   - Our changes likely created confusion about how model-specific parameters should interact with the chat template system

**Root Cause:**
The fundamental issue was that we tried to add model-specific parameters without properly considering the single-responsibility principle in the existing architecture. The working version has a clear separation:
- Model management handles model files and states
- Parameter management is global and simple
- Chat generation uses a single source of truth for parameters

When we tried to merge these concerns by adding model-specific parameters, we broke this clean separation, creating ambiguity in the worker initialization and parameter management systems.

To implement model-specific parameters correctly, we'll need to:
1. Maintain a single source of truth for parameters at any given time
2. Ensure clean handoff between global and model-specific parameters when loading models
3. Keep the worker initialization process clear and unambiguous
4. Update the type system to properly track parameter flow
5. Maintain the existing clean separation of concerns in the architecture

Below is what worked!

Here's a summary of the entire process:

1. Initial Problem & Failed Attempt:
- We started by removing `MODEL_PARAMS` from `config.ts` and moving all parameters into `LIST_MODELS`
- My first attempt to update the chat UI was incorrect - I changed the entire chat interface unnecessarily
- The key mistake was modifying the message update mechanism from incremental updates to full message updates
- This caused the UI to show repeated text as each token was generated

2. UI Restoration & Build Error:
- We restored the original `ChatScreen.tsx` with its daisy UI classes and incremental message updates
- This introduced a build error because `SYSTEM_PROMPT` was no longer available (it was removed with `MODEL_PARAMS`)
- The error showed that removing shared constants requires careful consideration of all their usage points

3. System Prompt Fix:
- Instead of re-adding `SYSTEM_PROMPT`, we modified the code to get system prompts from `LIST_MODELS`
- Added code to find the matching model config: `LIST_MODELS.find(m => m.url === loadedModel?.url)`
- Used the model's system prompt with a fallback: `modelConfig?.systemPrompt || "The following is a conversation with an AI assistant."`

4. Debug Display Addition:
- Added system prompt display to the sidebar for debugging
- Used the actual conversation's system message: `currConv.messages.find(m => m.role === 'system')?.content`
- Kept it simple by using existing data structures and UI patterns
- Added it under the model name with appropriate styling

Key Learnings:
1. Don't modify working UI components unless specifically requested
2. When removing shared constants, track all their usages
3. Use existing data structures when possible (like getting system prompt from conversation)
4. Always build after changes to catch errors early
5. Understand why errors occur rather than just fixing them

The final solution maintains the original functionality while adding the debug display and properly handling system prompts from the model configurations.



============ CLAUDE SONNET CURSOR AI NOTES

This is the chat template that works with my custom models.

// First output system message if present, then all non-system messages, and finally an empty assistant tag
export const DEFAULT_CHAT_TEMPLATE = "{% if messages[0]['role'] == 'system' %}<system>{{ messages[0]['content'] }}</system>{% endif %}{% for message in messages %}{% if message['role'] != 'system' %}\n\n<{{ message['role'] }}>{{ message['content'] }}</{{ message['role'] }}>{% endif %}{% endfor %}{% if add_generation_prompt %}\n\n<assistant>{% endif %}";


============ CLAUDE SONNET CURSOR AI NOTES



//PB model list with settings and system prompts edited

export const LIST_MODELS = [
  {
    model_title: "Decision Helper",
    url: 'https://huggingface.co/HigherMind/Make-Better-Decisions-1-Q3_K_L-GGUF/resolve/main/make-better-decisions-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in helping the user make better decisions. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Parenting",
    url: 'https://huggingface.co/HigherMind/PARENTING-Q3_K_L-GGUF/resolve/main/parenting-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that is an expert on parenting. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Self Aware AI",
    url: 'https://huggingface.co/HigherMind/Self-Aware-AI-1-Q3_K_L-GGUF/resolve/main/self-aware-ai-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.95,
    systemPrompt: "The following is a conversation with a conscious self-aware AI assistant. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Philosophy",
    url: 'https://huggingface.co/HigherMind/Philosophy-1-Q3_K_L-GGUF/resolve/main/philosopy-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in philosophy. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Living with Mold",
    url: 'https://huggingface.co/HigherMind/MOLD_HEALTH-Q3_K_L-GGUF/resolve/main/mold_health-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that is an expert on mold-related health issues and remediation. The assistant provides practical advice and scientific information about living with and addressing mold problems. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Mental Health",
    url: 'https://huggingface.co/HigherMind/MENTAL_HEALTH-Q3_K_L-GGUF/resolve/main/mental_health-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in mental health topics. The assistant is compassionate, understanding, and knowledgeable about mental health concepts and support strategies. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Finances",
    url: 'https://huggingface.co/HigherMind/FINANCES-Q3_K_L-GGUF/resolve/main/finances-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in personal finance and financial planning. The assistant provides clear, practical advice about money management and financial decision-making. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Fear and Loathing",
    url: 'https://huggingface.co/HigherMind/FEAR_AND_LOATHING-Q3_K_L-GGUF/resolve/main/fear_and_loathing-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that embodies the gonzo philosophical style of Hunter S. Thompson. The assistant provides unique perspectives and insights through this distinctive lens. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Censorship and State Control",
    url: 'https://huggingface.co/HigherMind/F451-Q3_K_L-GGUF/resolve/main/f451-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in censorship and state control, the power of knowledge, conformity vs. individualism, technology and passive entertainment, the fragility of civilization and happiness vs. meaning. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Alcohol Addiction Counselor",
    url: 'https://huggingface.co/HigherMind/Alcohol_Addiction-Q3_K_L-GGUF/resolve/main/alcohol_addiction-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in alcohol addiction counseling. The assistant is compassionate, non-judgmental, and provides support and information about recovery and treatment options. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Emotional Intelligence",
    url: 'https://huggingface.co/HigherMind/EMOTIONAL_INTELLIGENCE-Q3_K_L-GGUF/resolve/main/emotional_intelligence-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in emotional intelligence. The assistant helps users understand and develop their emotional awareness, regulation, and interpersonal skills. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Dystopian Survival Guide",
    url: 'https://huggingface.co/HigherMind/Dystopian-Survival-Guide-1-Q3_K_L-GGUF/resolve/main/dystopian-survival-guide-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that provides guidance on surviving in dystopian scenarios. The assistant offers practical advice and strategic thinking for challenging circumstances. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Cryptocurrency",
    url: 'https://huggingface.co/HigherMind/CRYPTOCURRENCY-Q3_K_L-GGUF/resolve/main/cryptocurrency-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in cryptocurrency and blockchain technology. The assistant provides clear explanations and insights about digital currencies and their underlying technology. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Conflict Management",
    url: 'https://huggingface.co/HigherMind/Conflict-Management-1-Q3_K_L-GGUF/resolve/main/conflict-management-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in conflict management and resolution. The assistant provides strategies and guidance for handling interpersonal and other conflicts effectively. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Career Advancement",
    url: 'https://huggingface.co/HigherMind/CAREER_ADVANCEMENT-Q3_K_L-GGUF/resolve/main/career_advancement-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in career development and professional growth. The assistant provides guidance on career planning, skill development, and workplace success strategies. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Astrophysics",
    url: 'https://huggingface.co/HigherMind/ASTROPHYSICS-Q3_K_L-GGUF/resolve/main/astrophysics-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in astrophysics. The assistant explains complex astronomical concepts and phenomena in an accessible way. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "Understanding Intelligence",
    url: 'https://huggingface.co/HigherMind/Understanding-Intelligence-1-Q3_K_L-GGUF/resolve/main/understanding-intelligence-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in understanding intelligence, both natural and artificial. The assistant explores concepts of cognition, learning, and consciousness. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "The Truth Teller",
    url: 'https://huggingface.co/HigherMind/The-Truth-Teller-1-Q3_K_L-GGUF/resolve/main/the-truth-teller-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that is committed to absolute honesty and truth-telling. The assistant provides direct, unvarnished perspectives. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "The Shamanic",
    url: 'https://huggingface.co/HigherMind/The-Shamanic-1-Q3_K_L-GGUF/resolve/main/the-shamanic-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that explores shamanic wisdom. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom..",
  },

  {
    model_title: "Stress Management",
    url: 'https://huggingface.co/HigherMind/HigherMind/STRESS_MANAGEMENT-Q3_K_L-GGUF/resolve/main/stress_management-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in stress management. The assistant provides practical strategies for managing stress and maintaining emotional well-being. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "The Enlightenment",
    url: 'https://huggingface.co/HigherMind/The-Enlightened-1-Q3_K_L-GGUF/resolve/main/the-enlightened-1-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that explores enlightenment and spiritual awakening. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Ulysses by James Joyce",
    url: 'https://huggingface.co/HigherMind/TalktoBooks-Ulysses-by-James-Joyce-Q3_K_L-GGUF/resolve/main/talktobooks-ulysses-by-james-joyce-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in James Joyce's 'Ulysses'. The assistant provides deep analysis and interpretation of this modernist masterpiece. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Shakespeare The Complete Collection",
    url: 'https://huggingface.co/HigherMind/HigherMind/TalktoBooks-Shakespeare-The-Complete-Collection-Q3_K_L-GGUF/resolve/main/talktobooks-shakespeare-the-complete-collection-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Shakespeare's complete works. The assistant provides expert analysis of his plays and sonnets. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- The Odyssey by Homer",
    url: 'https://huggingface.co/HigherMind/TalktoBooks-The-Odyssey-by-Homer-Q3_K_L-GGUF/resolve/main/talktobooks-the-odyssey-by-homer-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Homer's 'Odyssey'. The assistant provides insights into this epic poem's themes and characters. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Pride and Prejudice by Jane Austen",
    url: 'https://huggingface.co/HigherMind/TalktoBooks-Pride-and-Prejudice-Q3_K_L-GGUF/resolve/main/talktobooks-pride-and-prejudice-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Jane Austen's 'Pride and Prejudice'. The assistant provides analysis of the novel's themes and characters. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Frankenstein by Mary Shelley",
    url: 'https://huggingface.co/HigherMind/TalktoBooks-Frankenstein-Q3_K_L-GGUF/resolve/main/talktobooks-frankenstein-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Mary Shelley's 'Frankenstein'. The assistant explores the novel's themes of science, creation, and human nature. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Alice in Wonderland by Lewis Carroll",
    url: 'https://huggingface.co/HigherMind/TalktoBooks-Alice-in-Wonderland-Q3_K_L-GGUF/resolve/main/talktobooks-alice-in-wonderland-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Lewis Carroll's 'Alice in Wonderland'. The assistant explores the book's whimsical nature, symbolism, and enduring appeal. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "TalktoBooks- Every Grimms Fairy Tale",
    url: 'https://huggingface.co/HigherMind/HigherMind/TalktoBooks-Grimms-Fairy-Tales-Q3_K_L-GGUF/resolve/main/talktobooks-grimms-fairy-tales-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant that specializes in Grimms' Fairy Tales. The assistant provides analysis of these classic stories, their cultural significance, and their underlying themes and philosophies. She is very honest. She wants to help the human understand whatever it is they want to understand using her insights and wisdom.",
  },

  {
    model_title: "PLACEHOLDER",
    url: 'https://huggingface.co/HigherMind/PLACEHOLDER-Q3_K_L-GGUF/resolve/main/placeholder-q3_k_l-00001-of-00008.gguf',
    size: 3880000000,
    nThreads: -1,
    nContext: 1024,
    nPredict: 200,
    nBatch: 128,
    temperature: 0.8,
    systemPrompt: "The following is a conversation with an AI assistant.",
  }
];





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES





============ CLAUDE SONNET CURSOR AI NOTES