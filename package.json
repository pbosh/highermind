{
  "name": "highermind",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r video_particles/* dist/ && cp -r functions dist/",
    "dev": "wrangler pages dev video_particles --compatibility-date=2024-01-01"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
} 